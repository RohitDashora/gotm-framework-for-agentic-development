# Audit report — content-claim / decision-fidelity — 2026-06-15

## Target audited
File: `/Users/rohit.dashora/fe-vibe/gotm-framework-for-agentic-development/templates/PROTOCOL.md.template`
Unit: U64 — add the "a decision change can invalidate a prior pass" rule (the L22 re-audit-dependents gap) to the protocol template
Oracle: `/Users/rohit.dashora/fe-vibe/gotm-framework-for-agentic-development/.gotm/DECISIONS.md` → **D21**
Cross-check copies: `/Users/rohit.dashora/fe-vibe/gotm-framework-for-agentic-development/.gotm/PROTOCOL.md` (meta-example) · `/Users/rohit.dashora/fe-vibe/gotm/templates/PROTOCOL.md.template` (plugin mirror)

## Method
Read `prompts/audit.md` (method), `.gotm/DECISIONS.md` D21 (spec), all three PROTOCOL files (target + two synced copies), and the `.gotm/LEDGER.md` U64 row + ledger schema. Ran the 5-point checklist plus the four D21-specific verifications and the three-way drift cross-check. Byte-level equality of the rule paragraph and the lint bullet was checked programmatically across all three files.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None. (The three synced copies — framework template, meta-example, plugin mirror — carry the rule paragraph and the stale-by-decision lint **byte-identical**. No drift. Programmatic equality confirmed: `framework-template == meta-example == plugin` for both inserts.)

### LOW severity
- **"Inputs cite the changed decision" leans on a convention the ledger template states loosely.** The new rule (target L77) keys re-checking off "every `done` unit whose `Inputs` cite the changed decision." `templates/LEDGER.md.template` defines the `Inputs` column as "prior units whose outputs this one consumes (foundation gate)" — i.e. nominally `U<n>` references, not `D<n>`. In practice the repo's own ledger does put decisions in `Inputs` (U64's row reads `Inputs: D21`), so the rule is operable and the spec (D21) uses the same phrasing verbatim — this is a faithful transcription, not an authoring error. Flagged only because a cold reader who took the `Inputs`-column definition literally could miss that decisions belong there. Cosmetic; fix-if-convenient (would be a `LEDGER.md.template` clarification, not a change to U64's output, which is frozen-on-pass).

### UNVERIFIED
- None. All five checklist points and all four D21-specific points were mechanically checkable against the supplied oracle and copies.

## Detailed checks

**1. Existence.** PASS. Target exists at the ledger's stated path (`templates/PROTOCOL.md.template`); LEDGER U64 row Output cell matches exactly. 178 lines, intact.

**2. Spec match (D21 acceptance criteria).** PASS on all three sub-clauses:
- (a) *append a new timestamped `D<n>`, never silently edit* — present: "append a new timestamped `D<n>` — never silently edit the old entry (see the pre-edit check)" (L77). The explicit "(see the pre-edit check)" back-reference satisfies the D21 requirement to tie it to the append-don't-edit mechanism.
- (b) *re-check every `done` unit whose `Inputs` cite the changed decision against the new one* — present verbatim in intent: "**re-check every `done` unit whose `Inputs` cite the changed decision against the new one**" (L77).
- (c) *non-conforming output reverts `Audit` to `pending` or spawns a fix unit* — present: "if its output no longer conforms, that unit's `Audit` reverts to `pending` (re-audit independently) or the gap becomes a fix unit" (L77).
- The rule is correctly placed in the **Audit gates** section (the `## Audit gates` header is the only one; rule sits between "Findings become units, not edits" and "Deferral stays honest").
- The trailing sentence ("A `PASS` was true under the decision as it stood …") matches D21's "A `PASS` is true only under the decision as it stood when the unit was audited."

**2b. Stale-by-decision lint.** PASS. The **Resilience → session-start reconciliation** list (L135) contains "- **Stale-by-decision lint:** a `done` unit whose cited `D<n>` was later superseded/refined and that has not been re-checked against the new decision → flag it for re-audit before downstream relies on it. See *Audit gates*." Consistent with the rule, correctly placed directly after the existing **Audit-gate lint** bullet, and cross-links back to *Audit gates*.

**3. Cross-reference integrity.** PASS. The rule's one cross-reference ("see the pre-edit check") resolves to the **Anti-drift safeguards → Pre-edit check** section, item #2 of which is the append-don't-edit-decisions rule. The lint's "See *Audit gates*" resolves to the section that now contains the rule. D21 exists in `DECISIONS.md` (status: locked) and says what U64 claims. LEDGER U64 row cites D21 as its input. (See LOW finding re: the `Inputs`-column convention.)

**4. Internal consistency.** PASS — the new rule *complements* rather than contradicts:
- vs. **Pre-edit check #2** (append-don't-edit `DECISIONS.md`): the rule reinforces it ("append a new timestamped `D<n>` — never silently edit the old entry (see the pre-edit check)"). No conflict; it extends the consequence of a superseding append to the dependents.
- vs. **Cadence invariants** (one report per unit; own-report only; audit promptly): the revert-to-`pending`-then-re-audit path routes a stale unit back through the *same* per-unit independent audit machinery. Consistent — a re-audit produces that unit's own fresh `audits/<Uxx>.md`.
- vs. **Deferral rules** ("Deferral stays honest"): no overlap or contradiction; deferral governs the *first* audit's timing, this rule governs *re*-validation after a decision moves. They address different lifecycle points.
- vs. **Findings-become-units**: the "or the gap becomes a fix unit" branch reuses the existing findings-become-units mechanism rather than inventing a parallel one. Consistent.
- The lint and the rule agree (both: a `done` unit whose cited decision later changed and was not re-checked → re-audit before downstream relies on it).

**5. Decision fidelity.** PASS. The output honors D21 in full: the rule text, the lint, the placement in *Audit gates*, the "no docs-chapter edit this round" scope (no spurious doc edits introduced), and the synced-copies requirement (meta-example + plugin mirror) are all satisfied.

**No regressions.** PASS. Each file has exactly one rule occurrence, one lint occurrence, one `## Audit gates` header, one session-start-reconciliation header. Surrounding paragraphs ("Findings become units" before, "Deferral stays honest" after; "Audit-gate lint" before the new lint, "Record what reconciliation found" after) are intact in all three. No accidental deletion or mangling at either insertion point.

**Cross-check — synced copies (D21 "same turn" + plugin mirror).** PASS, no drift. Programmatic byte-equality confirms the rule paragraph is identical across framework-template (L77), meta-example (L77), and plugin mirror (L83); the lint bullet is identical across framework-template (L135), meta-example (L135), and plugin mirror (L141). The three differ only in unrelated pre-existing ways (the plugin copy references `/gotm:audit` and its `.gotm/hooks/` immutability hook elsewhere; relative `docs/`/`prompts/` paths differ by layout) — none of which touch the U64 inserts.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (one cosmetic LOW on the `Inputs`-column convention in `LEDGER.md.template`; the U64 output itself is faithful to D21 and free of drift across all three synced copies)
