# Audit report — content-claim (spec match / consistency / decision fidelity) — 2026-06-23

## Target audited
File: `prompts/audit.md`
Unit: U67

## Findings

### HIGH severity
- (none)

### MEDIUM severity
- (none)

### LOW severity
- (none)

### UNVERIFIED
- (none)

---

## Checklist run (U67 = implement G17: extend the default audit checklist 5 → 7)

**1. Existence.** PASS. Target exists at `prompts/audit.md` (the ledger's stated path for U67).

**2. Spec match — the 7-point checklist with checks 6 & 7 as promised, no stale "5-point" claim.** PASS.
- The "What to check" section now opens "Default to the **7-point checklist** below" (`prompts/audit.md:59`) and the code block header reads "What to check (default 7-point):" (`:61`).
- All seven items are enumerated `1.`–`7.` (`:62`–`:73`), with no gap and no eighth item.
- **Check (6) enforcement check** (`:67`–`:70`) is worded as D22 promised: "for each BEHAVIORAL decision the unit cites, is it held by a gate / config / assertion, or only prose? documented-but-unenforced is a finding (NOT the same as #5: fidelity asks 'does it honor the decision', enforcement asks 'is it gated')." It carries both required ideas — gate/config/assertion vs. prose, and the explicit distinctness from #5 decision-fidelity.
- **Check (7) multi-site claim check** (`:71`–`:73`) is worded as D22 promised: "any 'wired into BOTH / applied across N / replaced everywhere' claim is verified by grep/count, not trusted prose; expect a guard (test/assertion) per site." It carries the grep/count verification and the per-site guard expectation.
- **Stale-reference sweep (the explicit finding-trigger in the checks).** Searched the file for `5-point` / `five` / `5 point`. The only hit is line 79, inside the rationale callout: "… The 5-point core misses both." This is a *deliberate, correct historical* reference — the callout (`:77`–`:79`) explains *why* checks 6–7 were added (the prior 5-point core missed the two FAIL modes), not a claim that this prompt is a 5-point checklist. It is consistent with the protocol template's parallel rationale callout, which makes the same "5-point core misses both" point. Not a stale reference; not a finding. No `five`/`5 point` occurrences exist anywhere in the file.

**3. Cross-reference integrity.** PASS. The prompt makes no `D<n>`/`U<n>`/`Q<n>` citations that require resolution; its internal back-reference ("rule 4 of the protocol", "prompts/subagent-dispatch.md", `audits/<Uxx>.md`) are pre-existing and unaffected by the G17 edit.

**4. Internal consistency.** PASS.
- Header/enumeration agreement: the "default 7-point" header (`:59`, `:61`) matches exactly seven enumerated items (`:62`–`:73`). No off-by-one.
- The §6 return format (`:90`–`:117`) is checklist-agnostic — it specifies severity-tier sections (HIGH/MEDIUM/LOW/UNVERIFIED) and a verdict line, none of which assert a checklist length. Extending 5 → 7 does not contradict it.
- The "Audit kind" common-kinds list (`:31`–`:39`) and §5 severity tiers (`:81`–`:88`) are unchanged and uncontradicted by the new checks. The HIGH⇒FAIL / MEDIUM-LOW⇒PASS-FINDINGS / clean⇒PASS verdict mapping (`:117`) still holds.

**5. Decision fidelity — honors D22.** PASS.
- D22 → G17 bullet directs: extend the default checklist "5 → 7 points — (6) enforcement check (a behavioral decision the unit cites must be held by a gate/config/assertion, not only prose) + (7) multi-site-claim check (any 'wired into both / applied across N' claim is verified by grep/count, with a guard expected per site)." The prompt's checks 6 and 7 match this on every point.
- **Consistency with the protocol template (the oracle's parallel requirement).** `templates/PROTOCOL.md.template` (Audit gates, "default 7-point checklist" paragraph) words the same two checks: (6) enforcement check — gate/config/assertion vs. prose, with the "decision fidelity asks *does it honor*, this asks *is it held by something other than good intentions*" distinction; (7) multi-site claim check — "wired into both X and Y / applied across N sites / replaced everywhere" verified by grep/count with a per-site guard. The prompt's wording is the consistent prose-compressed form of the same two checks (same substance, same distinction from #5, same grep/count + per-site-guard expectation). No divergence.

**6. Enforcement check (this audit's own check 6).** N/A as a finding. U67 is a documentation/prompt unit; D22's G17 is itself a "documented discipline" item, not a behavioral decision requiring a runtime gate in *this* repo (the runtime enforcement is explicitly deferred to plugin v2.6.0 per D22). The "enforcement" of the checklist here is its presence in the paste-able prompt, which is satisfied.

**7. Multi-site claim check (this audit's own check 7).** PASS. D22 requires G17 to land in *two* sites: `templates/PROTOCOL.md.template` (Audit gates) and `prompts/audit.md`. Verified by grep that both sites carry the 7-point checklist with checks 6 & 7: the protocol template's "default 7-point checklist" paragraph (grep hit, single consolidated sentence) and `prompts/audit.md:59-73`. U67's scope is only the `prompts/audit.md` site; the protocol-template site is U65's scope (a separate unit/audit). Both are present and consistent — no half-applied multi-site fix.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
