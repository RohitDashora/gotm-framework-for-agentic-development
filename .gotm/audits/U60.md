# Audit report — content-claim + decision-fidelity + render — 2026-06-15

## Target audited
File: `docs/06-learning-across-projects.md`
Unit: U60 — new concept chapter 6, "Learning across projects."

Independence: this auditor did not author U60. Audit run fresh against the oracle
(D20, chapters 4–5, `prompts/outcome-analysis.md`, `templates/LEARNINGS.md.template`,
plus `README.md` for cross-reference integrity).

## Method
5-point checklist (existence · spec-match · cross-reference integrity · internal
consistency · decision fidelity) + a render check on the Mermaid diagram + the five
named D20 fidelity sub-checks (a)–(e).

---

## Findings

### HIGH severity
None.

### MEDIUM severity
None.

### LOW severity

- **"single command" left unnamed where D20 names `/gotm:learn`.** Chapter line 76
  says the producer is "in adopter tooling, a single command" but does not name it.
  D20 names the plugin command `/gotm:learn`. This is *not* a defect — D12/D20 keep the
  framework prompts-not-a-runtime and route the concrete command to the (private) plugin,
  so naming a plugin-specific slash command in the platform-neutral docs would arguably be
  the wrong call. Flagged LOW only as an awareness item; no fix recommended.

- **Producer-set is named in two places with slightly different emphasis.** The intro to
  §"What is built" (line 76) credits the producer to `outcome-analysis.md` +
  `LEARNINGS.md.template`; the closing pointer (line 80) re-cites only
  `outcome-analysis.md` as "the producer prompt." Both are correct (the prompt is the
  active half, the template scaffolds it); the asymmetry is stylistic, not contradictory.
  Cosmetic.

### UNVERIFIED
None. All claims were checkable against the oracle and the on-disk repo.

---

## Detailed check results

**1. Existence.** PASS. Target exists at the ledger's stated path
(`docs/06-learning-across-projects.md`, 7.4k, last_updated 2026-06-15, matching D20's date).

**2. Spec match.** PASS. D20 promised a chapter that documents a bottom-up three-level
learning layer with a defined record format, confidence ladder, and the
built-vs-described boundary. All present, in five well-formed sections plus a render
diagram and a back-to-README footer. Chapter voice matches ch4/ch5 (opening callback to
prior chapters; "the discipline stays small, as ever" mirrors ch5's "the discipline is
small"; "→ Back to the repository README" footer style matches ch5 line 64 / ch4 line 66).
Length (~7.4k) is in family with chs 4–5.

**3. Cross-reference integrity.** PASS.
- `prompts/outcome-analysis.md` — exists; its self-description ("the *producing* half of
  the cross-project learning loop", reads DECISIONS/audits/LEDGER, emits candidate records,
  candidate-only confidence) matches how the chapter describes it. Consistent.
- `templates/LEARNINGS.md.template` — exists; it implements the one-record-two-consumers
  model (Index + Records + merge model), the candidate→validated→core ladder, and the
  contradiction-demote decay path exactly as the chapter narrates. Consistent.
- "the auditor ≠ author rule from chapter 4" — verified in ch4 (independence section +
  the thread-through closing line). Accurate.
- "the same boundary chapter 4 drew around the enforcement hook" — verified in ch4 line 24
  ("platform binding and lives in adopter tooling ... not in the platform-neutral
  framework; ... does not ship a runtime"). Accurate cross-reference.
- `../README.md` link target exists. README §"Concept chapters" lists ch6 with a matching
  description; README repo-tree and "docs/ 6 concept chapters" count is consistent with
  the chapter's existence and with D20's "README chapter list updated" consequence.

**4. Internal consistency.** PASS. No contradiction with chapters 1–5:
- The "prompts-not-a-runtime / platform binding stops at producing records" stance aligns
  with ch4's enforcement-hook boundary and the README's platform-neutral framing.
- The "candidate is an anecdote; validation needs an *independent* project" framing reuses
  ch4's auditor≠author principle without restating it differently.
- The producer = "one pass at the end of a project" framing aligns with ch5's session loop
  and "the discipline is small" thesis; no scope creep that would conflict with the
  five-primitive model (D9).
- No claim contradicts the in-project (single-mission) framing of chs 1–5; the chapter is
  explicitly additive ("a second principle, built on the same mechanism").

**5. Decision fidelity to D20.** PASS on all five named sub-checks:
- **(a) three levels, bottom-up.** §"Bottom-up, in three levels" names L1 project /
  L2 user-harness / L3 enterprise, explicitly "bottom-up," matching D20 verbatim. The
  Mermaid encodes the same bottom-up flow (Produce ==> Pool ==> KG; Pool/KG -.-> Consume).
- **(b) producer shipped; consume + L2/L3 described-not-shipped.** Line 76 states the
  producing half of L1 is "a concrete, paste-able step" (the two shipped files) and that
  "Consuming, and the user- and enterprise-level aggregation ... are the scaling path ...
  it does not ship the store or the graph." Exact match to D20's scope clause.
- **(c) platform-neutral / no runtime shipped.** Line 76 closes "those are platform
  bindings, the same boundary chapter 4 drew around the enforcement hook." Honors D12/D20.
- **(d) ladder candidate→validated→core; no self-promotion past candidate;
  contradiction→demote.** §"Confidence is the bridge" lists the ladder verbatim, states
  "A candidate cannot promote itself to validated ... promotion requires an independent
  confirmation," and "a learning a later project contradicts ... is flagged for review and
  demoted ... the decay path." All three rules present and faithful.
- **(e) one-record-two-consumers (record + generated index).** §"The format: one record,
  two consumers" separates the structured, mergeable **record** (claim merge key, kind,
  tags, fix, scope, appendable evidence[], confidence) from the generated **index** of
  one-line entries with detail-on-demand. Matches D20's format clause and the template.

**Render check (Mermaid).** PASS. Extracted the chapter's `flowchart TB` block and rendered
it with `mmdc` (mermaid-cli): exit 0, valid SVG produced, no syntax errors. Subgraphs
(L1 with `direction LR`, L2, L3), node labels with `<br/>` and bold markup, solid `==>`
edges with labels, and dotted `-.->` edges all parse. Diagram semantics match the prose
(Produce → Pool candidate records; Pool → KG validated; Pool/KG → Consume available/core).

---

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (both findings are cosmetic LOW; no blocking or quality-degrading issue)
