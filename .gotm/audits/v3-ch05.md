# Audit — Chapter 5: Scaling & economy

**Target:** `docs/05-scaling-and-economy.md`
**Oracle:** `V3-DESIGN.md` (§6a fan-out/fan-in, §6b token economy, §10 decisions)
**Continuity context:** `docs/04-the-loop.md` (upstream), `docs/06-keeping-it-honest.md` (forward link)
**Auditor:** independent (did not author the chapter)
**Date:** 2026-06-26

---

## Tier 1 — Existence

PASS. The target file exists, is non-empty, titled `# Scaling & economy` (a real, descriptive title matching §8/§10's "Scaling & economy"), and is a coherent, finished chapter with an intro, two main sections (fan-out/fan-in, token economy), sub-sections, and a closing forward link.

## Tier 2 — Spec match (parts A + B, length, title, forward link)

PASS on substance for both A and B. One MEDIUM on length.

**Part A — fan-out/fan-in.** All required elements present:
- *Minimize barriers; default to pipeline* — "**Minimize barriers; default to pipeline.**" (L9); wall-clock "collapses to the **slowest single chain** through the DAG, not the sum of stages" (L9).
- *Barrier earned only by genuine all-upstreams joins* — "A **barrier is earned, not assumed.** It is justified only when a downstream genuinely needs *all* of its upstreams at once" (L11), with the earned/not-earned table (L13–18).
- *The hard rule* — present and prominent (see Tier 3).
- *Terse pointer-results* — "workers return **terse structured results** — a pointer plus a few index facts ... never prose, never the body" (L32).
- *Fan-out as a tree* — "**Fan-out is a tree, not a flat list.**" (L38).
- *Backpressure* — "**Backpressure.** Fan-out is bounded by a concurrency cap." (L39).
- *Explicit barrier-failure policy* — "**Explicit barrier-failure policy.** ... **retry** ... or **drop-and-continue** ... Never an unannounced partial merge." (L40).
- *Order-independent merges* — "**Order-independent merges.**" (L41).

**Part B — token economy = worker minimalism, not project budgets.** All required elements present:
- *Worker minimalism / lean workers* — "**Workers are kept lean.** ... exactly the bounded inputs it actually consumes, plus its spec and constraints — and nothing else. **Never** the whole ledger. Never sibling outputs ... Never the conversation." (L47).
- *Driver may be larger; don't starve it* — "**The driver may be larger — and that is legitimate.** ... Do **not** starve it" (L49).
- *Amortized batching / partition-worker* — L53.
- *Audit weight by risk* — L54.
- *Cheap hot tier* — L55.
- *Model tiering* — L56.
- *Project budgets/forecasting/budget-governed loop deliberately NOT adopted* — dedicated §"What we deliberately did *not* build" (L58–60).
- *Honest "not necessarily fewer tokens"* framing — L62.

**Title:** real and correct (`# Scaling & economy`).

**Forward link to ch6:** Present and correct — "The next chapter turns to ... **keeping it honest** — structural audit independence, the authored-done / verified-done distinction, and the freeze." (L66). Matches §8 ch6 exactly.

**Length (MEDIUM):** Spec band is ~1000–1400 prose words. Measured prose (excluding tables and headings) is ~1850 words; raw `wc -w` is 1960. The chapter runs roughly 30–45% over the stated band. Content is on-spec and not padded with filler, but it materially exceeds the ceiling — flagging as MEDIUM (a length-band miss, not a content defect). Note the blueprint's own coherence-pass list (§11) already tracks "ch7 ~150w over"; ch5's overage is larger and was not pre-flagged.

## Tier 3 — Decision fidelity (CRITICAL → any violation is HIGH)

PASS. Every decision-critical requirement is rendered faithfully; no HIGH violations.

- **The hard rule — stated correctly and prominently.** It is given its own H3 ("### The hard rule: fan-in is a fresh worker reading the store", L22), explicitly framed as "the single most important rule in the chapter" (L24), and set as a blockquote: "**A fan-in is a fresh worker that reads the N outputs from the store and emits the merged output. The driver receives one pointer — never the N bodies.**" (L26). The "fresh worker reading the store" and "driver never holds N results" halves are both correct and verbatim-faithful to §6a / §10 Q-v3-7. "At no point do the N bodies enter the driver's context." (L28).
- **Rationale = re-concentration/monotonicity at each join.** Correct and explicit: "If it pulls N full bodies in to merge them, that work now lives in the long-lived context, and it does so **at every barrier**. ... That is monotonicity, reintroduced one join at a time" (L30). Matches §6a's "every barrier re-concentrates the work into the long-lived context — monotonicity, reintroduced at each join."
- **Project budgets/forecasting/budget-governed loop deliberately dropped.** Correct: "v3 has **no project token budgets, no DAG cost-forecasting, and no budget-governed loop.** We do not predict ... do not allocate a ceiling ... do not let a budget governor steer the scheduler." (L60), framed as "a decision, not an oversight" (L60). Matches §6b "Dropped by decision" and §10 Q-v3-6. The chapter does NOT advocate any of them.
- **Worker-minimalism + driver-may-be-larger asymmetry.** Present and correctly framed as a deliberate asymmetry: "be frugal where there are many contexts; be generous where there is one." (L45); driver "may be larger — and that is legitimate" (L49). Matches §6b.
- **Honest token framing (not "saves tokens").** Present and correct: "**v3 does not necessarily spend fewer total tokens.**" (L62); the win is the spend being "**bounded** ... **attributable** ... **parallelizable** ... **tier-able**" — "That, not a lower bill, is the win." (L62). Matches §6b/§4 honest-limit framing.

## Tier 4 — Canonical terms

PASS. All required canonical terms present and used correctly: driver, worker, store, fan-out (L7, L38), fan-in (L22, L28, L38), barrier (L7, L11, L40), pipeline (L9, L11, L20), partition-worker (L53), amortized batching (L53), worker minimalism (L43, L45, L66), backpressure (L39), model tiering (L56), hot tier (L55), hot path (L3, L55), monotonic / monotonicity (L3, L30). Spark-as-analogy is appropriately absent from this chapter (ch2 owns the role mapping) — no drift.

## Tier 5 — Internal consistency, quality, continuity

PASS. No HIGH/MEDIUM defects.

- **Upstream continuity (ch4):** Clean. Opening recaps ch4's loop ("The previous chapter set the DAG in motion: the driver walks the graph, computes the ready set, dispatches workers, and collects results." L3) before extending it to many-units/wide-projects. Lineage-recompute correctly cross-referenced to ch4 (L40: "lineage recompute, chapter 4").
- **Cross-references:** All point the right way — ch1 (failure mode, L24), ch2 (driver-carries-index rule + re-hydration, L30/L49), ch3 (foundation→drafts gate, L20), ch4 (lineage, L40), ch6 (audit mechanics deferred, L54), ch7 (compaction mechanics deferred, L55). No contradictions.
- **Thematic spine:** The intro's claim that the parallelism fix and the economy fix "turn out to be the same shape" (L3) is paid off by the closing ("both reduce to the same rule, that the long-lived context holds the index and never the work" L66) — coherent framing, consistent with the blueprint's net principle (§4).
- **Quality:** High. The earned/not-earned barrier table (L13–18) carries weight efficiently; the "a merge is work. So a merge is a worker." beat (L30) is a crisp restatement of the hard rule. Terse-structured-result is correctly tied back to the fan-in rule (L32).

## Tier 6 — No encroachment (ch4 / ch6 / ch7)

PASS. Stays in its lane.

- **ch4 (loop mechanics):** Referenced as upstream recap only (L3) and for lineage recompute (L40); does not re-derive the scheduler loop.
- **ch6 (audit independence / verified-done):** Audit-weight-by-risk is treated *purely as an economy lever* with an explicit deferral — "(The independence and verdict mechanics are chapter 6's subject; here it is purely an economy lever.)" (L54). Correct boundary; no encroachment.
- **ch7 (compaction / resilience):** Hot-tier sizing referenced as an economy lever with explicit deferral — "(Compaction keeps the hot tier bounded over time; its mechanics are chapter 7.)" (L55). Correct boundary.

---

## Summary

`HIGH: 0 · MEDIUM: 1 · LOW: 0 · Verdict: PASS-FINDINGS`

MEDIUM-1: Length over band. Prose runs ~1850 words (raw ~1960) against the ~1000–1400 spec band — roughly 30–45% over. Content is on-spec and unpadded, but the ceiling is materially exceeded; recommend a tightening pass (or an explicit logged band-waiver) during the fan-in coherence pass. All decision-critical content (Tier 3) and both spec halves (Tier 2 A+B) are faithfully and prominently rendered.
