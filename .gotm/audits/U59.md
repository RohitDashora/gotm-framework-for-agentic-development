# Audit report — independent audit of U55–U58 (G12/G14/G13a fold) — 2026-06-12

> Produced by an **independent auditor agent** (did not author U55–U58). Skeptical, mechanical, read-only. No files other than this report were created or modified; the ledger was not stamped.

## Target audited
Files:
- `templates/PROTOCOL.md.template`
- `templates/LEDGER.md.template`
- `prompts/audit.md`
- `.gotm/PROTOCOL.md` (meta-example's own protocol)

Units: U55, U56, U57, U58 (consolidated; final consolidated audit per D19)
Oracle: `.gotm/LEDGER.md` (U55–U59 rows), `.gotm/DECISIONS.md` (D19)

## Method
Read all four targets in full; read the oracle (LEDGER U55–U59, D19). Grepped the distinctive phrases. Diffed the working tree against `HEAD` (last commit `2fa6ec3` = U52–U54) to isolate exactly the text U55–U58 introduced, so pre-existing prose is not mis-attributed to this change.

---

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity

- **L1 — Protocol gate-list (line 73) does not enumerate the new `superseded by U<yy>` audit value, though the cadence bullet just below it (line 82) describes it.** In both `templates/PROTOCOL.md.template:73` and `.gotm/PROTOCOL.md:73`, the **The gate** sentence lists the `Audit` vocabulary as `—` / `pending` / `deferred→U<n>` / `PASS→…` / `PASS-FINDINGS→…` / `FAIL→…` — it does **not** include `superseded by U<yy>`. That value is correctly added to `templates/LEDGER.md.template:38` and is described in the cadence "Own-report only" bullet (`…PROTOCOL.md:82`). Line 73 is a pre-existing line this change did not touch, so the protocol is complete *in aggregate* (the value is defined two bullets later), but a reader scanning the enumerated gate-list alone would not see it. Cosmetic completeness gap, not a contradiction. Fix if convenient: add `· superseded by U<yy>` to the line-73 enumeration in both protocol files.

- **L2 — The repo's own ledger stamps U55–U58 `Audit: covered by U59 (final consolidated)`, the exact "covered by another unit's audit" pattern the new G12 invariant forbids.** `.gotm/LEDGER.md:147–150`. This is **explicitly sanctioned** by D19's Transition note (`.gotm/DECISIONS.md:285`): prior consolidated audits "stand as history; per-unit auditing binds from here … This pass's own audit (below) is the final consolidated one." So it honors the decision and is not a fidelity defect. It is flagged LOW only because the ledger is the meta-example demonstrating the discipline, and the final consolidated stamp sits in visible tension with the very invariant being introduced. The ledger is *not* one of the four target files under this audit (the framework templates + audit.md + meta protocol are clean); noted for completeness. No action required — D19 covers it.

### UNVERIFIED
- None. All five checklist points were mechanically verifiable from the targets + oracle.

---

## Checklist results (5-point)

1. **Existence — PASS.** All four target files exist (`templates/PROTOCOL.md.template`, `templates/LEDGER.md.template`, `prompts/audit.md`, `.gotm/PROTOCOL.md`). Oracle files (`.gotm/LEDGER.md`, `.gotm/DECISIONS.md`) exist.

2. **Spec match — PASS.** Each required invariant is present in the files D19 names:
   - **G12 (3 invariants)** — `templates/PROTOCOL.md.template:79–83` and `.gotm/PROTOCOL.md:79–83` carry the **Cadence** section with all three: "One report per unit" (`:81`), "Own-report only" + `superseded by U<yy>` exception (`:82`), "Audit promptly" (`:83`). `prompts/audit.md:19` carries "One unit per audit" (= one-report-per-unit) and `:21` carries audit-promptly ("Do it promptly — right after the unit goes `done`, before starting the next"). `templates/LEDGER.md.template:40–42` carries "One audit + one report PER UNIT … OWN report (no 'covered-by' stamps) … promptly — don't let `pending` pile up."
   - **G14 (born `pending`/`in_progress`, never `done`)** — `templates/PROTOCOL.md.template:123` and `.gotm/PROTOCOL.md:123` in the Resilience section; `templates/LEDGER.md.template:45–47` in conventions ("A new unit is born `pending`/`in_progress`, NEVER `done`").
   - **G13a (module + test = one unit)** — `templates/PROTOCOL.md.template:29` and `.gotm/PROTOCOL.md:29` in Rule 2 ("a module and its test file are one unit — build-and-test is a single pass"); `templates/LEDGER.md.template:32–34` in conventions ("A module + its test file count as ONE unit").
   - **Both protocol files carry all three** (confirmed: `templates/PROTOCOL.md.template` and `.gotm/PROTOCOL.md` are byte-identical in the three edited regions per the diff).

3. **Cross-reference integrity — PASS.** D19 exists (`.gotm/DECISIONS.md:275`) and its Decision text matches what U55–U58 claim (G12 three invariants, G14 born-`in_progress`, G13a module+test grain; declined G13b). LEDGER rows U55, U56, U57, U58 (`.gotm/LEDGER.md:147–150`) each reference `D19` in their Inputs column; U58 also references U55. The markdown link targets in the edited regions resolve: `prompts/audit.md` exists for the template's neutral default reference, and `../prompts/audit.md` resolves from `.gotm/PROTOCOL.md`.

4. **Internal consistency — PASS.** The G12/G14/G13a wording is consistent across all four files (the `superseded by U<yy>` exception appears identically in both protocols at `:82`, in the LEDGER vocab at `:38`, and in `prompts/audit.md:19`; the "audit promptly / before starting the next" timing is consistent across the three places it appears). The new text does **not** contradict the existing Audit gates, deferral ("Audit deferral is sanctioned" `:59`, "Deferral stays honest" `:77`), Resilience crash-safe ordering, or Rule 2 — the cadence's "don't let `pending` pile up" coexists with the sanctioned `deferred→U<n>` named exception; G14 restates the same crash-safe write ordering already at `:115–121`; G13a refines (does not overturn) "one named output file." Only nit is L1 (the line-73 gate enumeration omits the new vocab value defined just below it).

5. **Decision fidelity / NEUTRALITY — PASS.** Honors D19. **Neutrality (critical) holds:** the new G12/G14/G13a text introduced by this change (diffed against HEAD) contains **no** `/gotm:` plugin-command reference and **no** vendor name in any of `templates/*`, `prompts/audit.md`, or `.gotm/PROTOCOL.md`. `prompts/audit.md` is fully vendor-neutral. The only "Claude Code plugin" mention in the protocol files is at line 107 ("for example, a Claude Code plugin"), which is **pre-existing** (authored under U23/D12, untouched by this change per the diff) and is correctly framed as an *example* of adopter tooling, not a command reference — D12 sanctions naming the plugin as the runtime boundary. G14's mention of "the immutability hook (if wired)" and "the plugin hook's deny message" (in D19/LEDGER) is hedged and tool-agnostic in the template ("if wired"), naming no command. No `/gotm:` leak was introduced.

---

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 2 · UNVERIFIED: 0
Verdict: **PASS-FINDINGS**   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

The G12/G14/G13a fold landed correctly and completely across all four files. Both protocol files carry all three invariants identically; the LEDGER template and audit prompt carry their respective slices; cross-references to D19 resolve; the platform-neutral templates and `prompts/audit.md` introduce no `/gotm:` or vendor leak. The two LOW findings (L1: the pre-existing line-73 gate enumeration omits the `superseded by U<yy>` value defined just below; L2: the repo's own ledger uses a "covered-by" stamp that D19 explicitly sanctions as the final consolidated transition) are non-blocking and do not affect the trustworthiness of the edited deliverables. The output is consumable downstream.
