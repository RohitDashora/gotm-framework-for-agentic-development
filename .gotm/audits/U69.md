# Audit report — structure / spec-match — 2026-06-23

## Target audited
File: `prompts/consult.md`
Unit: U69

## Method
Audited mechanically against the 7-point checklist (`prompts/audit.md`). Oracle: ledger row U69, `DECISIONS.md` D22 (G16 framework half), the symmetric producer `prompts/outcome-analysis.md`, the record schema in `templates/LEARNINGS.md.template`, the loop chapter `docs/06-learning-across-projects.md`, and the G16 source (`geniefy-v3/docs/GOTM-FEEDBACK.md` ~lines 124–127).

## 7-point results

1. **existence** — PASS. `prompts/consult.md` exists at the ledger's stated path (U69 row: target `prompts/consult.md`).
2. **spec match** — PASS. Every promised behavior is present (see itemized table below).
3. **cross-reference integrity** — PASS. All three internal links resolve: `outcome-analysis.md` (sibling in `prompts/`) exists; `../docs/06-learning-across-projects.md` exists from `prompts/`; the parenthetical reference to `docs/06` for the platform-binding boundary is accurate. Relative paths are correct for a file living in `prompts/`.
4. **internal consistency** — PASS. No internal contradictions; consistent with `outcome-analysis.md`, `templates/LEARNINGS.md.template`, and `docs/06` on the confidence ladder, the record schema, and the platform-binding boundary (see below).
5. **decision fidelity** — PASS. Honors D22's G16 resolution: ships the consume-side *step* symmetric to the produce side, explicitly states the store/index (L2/L3) stays a platform binding, and narrows D20's open "consume half."
6. **enforcement check** — PASS (n/a-leaning). U69 is a prose prompt, not a behavioral-gate unit; it cites no behavioral decision that requires a gate/config/assertion. The "empty pool is a valid, auditable result, not a silent skip" obligation is itself stated as an instruction to the consuming agent, which is the only enforcement a paste-able prompt can carry.
7. **multi-site claim check** — PASS (n/a). The unit makes no "wired into both / applied across N / replaced everywhere" claims to verify by count.

### Spec-match detail (each promised behavior)

| Promised (ledger / D22 / G16) | Present? | Where in consult.md |
|---|---|---|
| Run at project start, and again on new stack/surface/phase | Yes | L10–12; Output note "may be refreshed when the work moves to a new area" (L80) |
| The *consuming* mirror of `outcome-analysis.md` | Yes | L12–15 |
| Read across a POOL: configured list / sibling-repo glob / user-level `~/.gotm/learnings/` | Yes | L28–31 |
| Scan cheap one-line indexes, tag-filter, expand only matches | Yes | L37–56 (steps 1–4: name tags, scan indexes, tag-filter, expand) |
| Surface survivors to `CONSULTED.md` / `.gotm/CONSULTED.md` | Yes | L52–56, L74–80 |
| Honest about confidence (candidate = n=1; validated/core weigh more) | Yes | L58–69 |
| POOL/store is a platform binding NOT shipped here (only the step is) | Yes | L23–26 ("Where the pool lives is a platform binding, not fixed by this framework") |
| Empty pool / no-match is a valid, honest result, not a silent skip | Yes | L33–35, L76–80 ("an honest 'nothing relevant found' is a valid, auditable result") |

### Consistency-with-oracle detail (the three risk areas flagged)

- **Confidence ladder.** consult.md (L58–69) uses `candidate → validated → core` with the same definitions as `outcome-analysis.md` (L48–49), `templates/LEARNINGS.md.template` (L30, L39), and `docs/06` (L66–72). No contradiction.
- **Record schema.** consult.md step 4 (L48–51) expands `claim, fix, scope, evidence, confidence`; the index scan (L43–47) keys on tags. These are a strict subset of the template's schema (`id · claim · kind · tags · fix · scope · evidence · confidence (+ strength)`), naming exactly the fields a consumer reads. No field is renamed or invented. Consistent.
- **Platform-binding boundary.** consult.md (L23–26) draws the same boundary as D22 ("`prompts/consult.md` specifies the step, not the store") and `docs/06` (L78, "what this framework deliberately does not ship is the pool itself"). Consistent.

### Shape / symmetry with outcome-analysis.md

- Frontmatter: both carry `prompt / purpose / audience / license: Apache 2.0` (consult.md L1–5). Match.
- Sectioned guidance, honest-about-confidence section, single clear `## Output`: all present and mirror the producer's shape.
- The kind taxonomy referenced in step 5 (`prerequisite` → to-do, `gotcha`/`anti-pattern` → avoid, `pattern` → approach; L54–56) is a faithful subset of outcome-analysis's `kind` classification (gotcha/prerequisite/pivot/pattern/anti-pattern). `pivot` is not given a projection verb, which is reasonable — a pivot is historical context, not a forward action — and is not a contradiction.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **Bidirectional-link asymmetry (cosmetic).** consult.md links to `outcome-analysis.md` (L13, L82), but `outcome-analysis.md`'s Output section (its L52–53) does not link back to the now-existing `consult.md`. `docs/06` already wires both directions, so the loop is documented; the producer prompt simply predates U69. Not an output defect of U69 — the missing link is in the *other* file. Fix if convenient via a follow-on touch to `outcome-analysis.md`.

### UNVERIFIED
- None.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 0
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
