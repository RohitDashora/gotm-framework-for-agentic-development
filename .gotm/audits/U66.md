# Audit report — structure / decision-fidelity (7-point) — 2026-06-23

## Target audited
File: `templates/LEDGER.md.template`
Unit: U66

## Scope
U66 promised to implement finding **G15** (per D22) in `templates/LEDGER.md.template`:
1. `## Recent updates` documented as THE single recovery log, newest-first (prepend).
2. `## Active unit` documented as a one-line pointer to the current unit only — not a growing stack of dated status banners.
3. The conventions block warns that a second de-facto recovery log is a reconciliation smell, and points to PROTOCOL.md → Resilience.
4. (G18-related) a convention that unit-table rows are machine-parsed and must stay well-formed.

Oracle: `.gotm/DECISIONS.md` (D22), `geniefy-v3/docs/GOTM-FEEDBACK.md` (G15 ~L119-122, G18 ~L134-137), `templates/PROTOCOL.md.template` (Resilience → "One recovery log, one ordering"), `prompts/audit.md`.

## 7-point checklist results

1. **Existence** — PASS. Target exists at the ledger's stated path; 72 lines, reads cleanly.
2. **Spec match** — PASS. All four promised elements are present and in the correct sections:
   - Recovery-log + newest-first: `## Recent updates` comment, lines 32-35 ("THE recovery log — the single place a cold session reconstructs"; "NEWEST-FIRST: prepend each entry"; "Do not start a parallel dated stack anywhere else (esp. under `## Active unit`) — one log, one ordering").
   - Active-unit-as-pointer: `## Active unit` comment, lines 14-17 ("A POINTER to the current unit only — one line, overwritten each turn. NOT a growing stack of dated status banners").
   - Second-log smell + pointer to PROTOCOL: conventions block, lines 60-63 ("ONE recovery log, ONE ordering … A second de-facto log forming is a reconciliation smell. See PROTOCOL.md -> Resilience").
   - G18 machine-parsed-row convention: conventions block, lines 64-67 ("A unit-table row is also machine-parsed … Keep every `| U<n> |` row well-formed — correct column count, a valid status, a parseable Output cell … See PROTOCOL.md -> session-start reconciliation").
3. **Cross-reference integrity** — PASS. The template cites PROTOCOL.md sections (`-> Resilience`, `-> session-start reconciliation`, `-> Audit gates`, `-> Off-mission artifacts`), not D/U/Q numbers. Each pointer resolves: `templates/PROTOCOL.md.template` has a *Resilience* section (line 111) containing "One recovery log, one ordering" (line 117) and "Session-start reconciliation" (line 131) with a ledger-parse lint (line 139). No dangling reference.
4. **Internal consistency (incl. consistency with the PROTOCOL template)** — PASS. The load-bearing check. The LEDGER template's recovery-log rule does **not** contradict the PROTOCOL template's:
   - LEDGER (L60-63): "`## Recent updates` is the single recovery log and is newest-first; `## Active unit` is a one-line pointer, never a dated banner stack. A second de-facto log forming is a reconciliation smell."
   - PROTOCOL (L117): "`Recent updates` is *the* recovery log, and it is **newest-first** … `## Active unit` holds **only a pointer to the current unit** — never a growing stack of dated status banners."
   Same rule (Recent updates = the one log, newest-first; Active unit = pointer; second log = smell). The "prepend"/"newest-first" framing (LEDGER L32-35) matches PROTOCOL L117; the second-log lint reference (LEDGER L63) matches PROTOCOL L117/L138. No internal contradiction within the template either (the `## Active unit` example at L19 is a single one-line pointer, matching its own comment).
5. **Decision fidelity (D22)** — PASS. D22's G15 bullet requires: Recent updates = the recovery log, newest-first; Active unit = pointer only, never a banner stack; reconcile warns on a second de-facto log. The template honors all three for the LEDGER's part and correctly defers the *reconcile-warn* mechanism to PROTOCOL (the U65 half) via "See PROTOCOL.md -> Resilience". D22's G18 note (unit table is hand-edited + machine-parsed) is reflected at L64-67. No decision drift.
6. **Enforcement check** — PASS (n/a-leaning). This is a documentation template, not a behavioral unit. The G15/G18 behaviors it documents are enforced — to the extent they are — by the PROTOCOL session-start reconcile lints (second-recovery-log lint, ledger-parse lint; U65) and adopter tooling. The template introduces no documented-but-unenforced gate of its own; it points at the enforcing section rather than claiming enforcement.
7. **Multi-site claim check** — PASS. U66's text makes no "wired into both / applied across N" claim that would need grep/count verification. The one cross-artifact consistency concern (LEDGER vs. PROTOCOL wording) was verified directly by reading both files under check 4.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- None material. (Observation, not a defect: the G15 guidance now appears in two comment blocks — the `## Recent updates` block (L32-35) and the conventions block (L60-63) — plus the `## Active unit` block (L14-17). This is intentional placement-at-point-of-use, fully consistent across all three; noted only for completeness, no action needed.)

### UNVERIFIED
- None.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
