# Audit report — content-claim (behavioral-accuracy fix) — 2026-06-23

## Target audited
File: `docs/06-learning-across-projects.md`
Unit: U68 (follow-on revising U60's output, per D22 / finding G16)

## What U68 promised
Revise the "What is built, and what is the path" section so it states that **both
halves of the level-1 loop are concrete, paste-able steps** (produce =
`prompts/outcome-analysis.md` + `templates/LEARNINGS.md.template`; consume =
`prompts/consult.md`), while the **pool/store/index** (L2/L3 aggregation) remains
an unshipped platform binding. Reference `prompts/consult.md`. Bump `last_updated`
to 2026-06-23. Behavioral-accuracy fix: the prior (U60) text treated the consume
half as part of the unshipped scaling path; after the edit that stale framing must
be gone.

## Checks run (default 7-point)

1. **existence** — PASS. Target exists at the ledger's stated path
   (`docs/06-learning-across-projects.md`).
2. **spec match** — PASS. The revised "What is built, and what is the path" section
   (lines 74–82) now opens *"Both halves of the level-1 loop are concrete, paste-able
   steps"* and explicitly names the consume step `prompts/consult.md` (line 76) plus
   the closing pointer (line 82, *"the consumer prompt is `prompts/consult.md`"*).
   Produce-side artifacts (`outcome-analysis.md`, `LEARNINGS.md.template`) are still
   cited. The store/index is held out as the only unshipped piece (line 78). The
   git diff vs HEAD (committed U60) confirms exactly this rewrite.
3. **cross-reference integrity** — PASS. All relative links resolve from `docs/`:
   `../prompts/consult.md`, `../prompts/outcome-analysis.md`,
   `../templates/LEARNINGS.md.template`, `../README.md` — all four files exist.
   The chapter's description of `consult.md` ("the start-of-project step that scans
   the pool's indexes, tag-filters to the work at hand, and surfaces the few
   relevant records") matches `consult.md`'s own spec verbatim in substance
   (start-of-project, scan-index, tag-filter, surface survivors). The "bootstrap
   pull that consults the pool at project start" claim is honored by `consult.md`
   line 10 ("Run this **at the start of a project** (in bootstrap)").
4. **internal consistency** — PASS. Targeted contradiction sweep for any remaining
   sentence treating the consume half as unshipped/described-only/scaling-path-only
   returned **zero** hits. The old U60 sentence ("Consuming, and the user- and
   enterprise-level aggregation above it, are the scaling path … it does not ship
   the store or the graph") has been removed; the unshipped claim now applies only
   to "the **pool itself** — *where* the learnings live and *how* they are indexed."
   No contradiction between "consume STEP ships" and "consume STORE doesn't."
5. **decision fidelity** — PASS. Honors D22's framing precisely: G16 "narrows D20's
   open 'consume half' from undescribed to specced-and-paste-able" — the chapter now
   says the consume *step* ships as a prompt while "the consume **store/index**
   (L2/L3) stays a platform binding (the D20 boundary is unchanged)." The chapter's
   line 78 mirrors this: "The two prompts specify the *steps*; the store is a
   platform binding, the same boundary chapter 4 drew around the enforcement hook."
6. **enforcement check** — N/A (PASS). This is a documentation chapter; it cites no
   behavioral decision that a gate/config/assertion would need to hold. The
   "platform binding" framing is the intended non-enforcement and is stated as such.
7. **multi-site claim check** — PASS. The chapter's load-bearing claim is "Both
   halves … are concrete, paste-able steps." Verified by count: produce artifacts
   (`outcome-analysis.md`, `LEARNINGS.md.template`) and consume artifact
   (`consult.md`) all exist on disk. No "applied across N / wired into both" claim
   that a grep failed to corroborate.

## Findings

### HIGH severity
- none.

### MEDIUM severity
- none.

### LOW severity
- none.

### UNVERIFIED
- none.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 0 · UNVERIFIED: 0
Verdict: PASS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)

Notes (out-of-kind, non-blocking): the U68 edit is present in the working tree but
not yet committed (git log for `docs/06` still tops out at the D20/D21 commit
`f26c695`). This is a process observation, not a content defect — the unit's
*output* matches spec. The `.gotm/audits/U68.md` Audit cell in `LEDGER.md` reads
`pending`; the orchestrator should stamp it `PASS→.gotm/audits/U68.md`.
