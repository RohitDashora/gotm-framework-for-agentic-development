# Fix note — MEDIUM doc-accuracy findings from the v3 build audit — 2026-06-29

Resolved the three MEDIUM doc-accuracy findings from `.gotm/audits/v3-build-consistency.md` (M1) and `.gotm/audits/v3-build-governance.md` (M1, M2). (1) **`docs/06-keeping-it-honest.md`** — reframed the "enforcement is real, not honor-system / the v3 hook honors follow-on ownership" passage so it no longer reads as documented-but-unenforced in this platform-neutral framework: kept the idea, but made explicit that the immutability hook is a runtime binding **shipped by the GOTM plugin**, that *this* repo only *describes* the enforcement path and relies on doc-level discipline (driver + PROTOCOL), and that enforcement is "real in the runtime, described here" (cross-ref ch9 / PROTOCOL on what ships vs. what is a binding) — consistent with README/PROTOCOL/ch9 elsewhere. (2) **`V3-DESIGN.md`** — corrected the self-contradicting diagram count from "8" to **9** (matching the `mmdc 9/9` in the same line; noted ch3 and ch5 carry two each) at both §11 spots; and corrected the §11 audit-coverage overclaims so ch4/ch8/ch9 are described as validated *within the fan-in coherence pass* rather than standalone-gated, fixing the "all 9 chapters independently gated / zero HIGH/FAIL" prose in both the DOCS-PHASE-COMPLETE and CONTENT-COMPLETE lines, and added a one-line note that the build-process audit (`.gotm/audits/v3-build-*.md`) served as the deferred independent audit of the previously-unaudited set (PROTOCOL surface, prompts, tooling, ch4/8/9). (3) **`gotm/commands/bootstrap.md`** (plugin) — added the omitted `LEARNINGS.md.template` to Step 1's drop-list (alongside CONSULTED, both created if present), bringing the list to all 17 templates; and corrected the secondary nit that the loop "points at" `compact_ledger.py` — re-attributed the script reference to `hooks/README.md` and PROTOCOL.md's *Ledger compaction* section, while keeping that the loop's checkpoint step *performs* the compaction.

**Wording judgments:** in ch6 I chose "enforcement can be made real… but where it lives matters" plus an explicit framework-vs-runtime split rather than the audit's terser suggested one-liner, because the original sentence was the load-bearing honesty claim and a bare caveat would still read as half-asserting live enforcement. In V3-DESIGN §11 I kept the risk-tiering note (already present) as the canonical explanation and made the summary lines defer to it, rather than re-litigating per-chapter verdicts. In bootstrap, I softened "points at" to "referenced by … the loop's checkpoint step performs this compaction" so the script's true home (hooks/README + PROTOCOL) and the loop's role (it does the compaction, doesn't name the script) are both accurate. Edited only the three named files.
