# Audit — Chapter 2: The architecture: driver / worker / store

**Target:** `docs/02-driver-worker-store.md`
**Oracle:** `V3-DESIGN.md` §3 + §4 (locked blueprint)
**Context:** `docs/01-the-problem-and-thesis.md` (continuity)
**Auditor:** independent (not the author)
**Date:** 2026-06-26

---

## CHECK 1 — Existence

PASS. Target exists, is non-empty (1408 words incl. the role table; 1273 prose-only excl. table), and is a coherent, finished chapter with real prose — not a stub or outline.

## CHECK 2 — Spec match (all four elements + table + length + forward link)

All four required elements present:

1. **Three roles + Spark analogy + lifetime/holds/never-holds.** PASS. The role table (lines 9–13) and the three prose paragraphs (lines 15–19) cover all three roles with the Spark analogy ("by analogy to Spark, which solved the same problem one layer down," line 5; per-role Spark mapping in the table: driver→driver, worker→executor, store→"HDFS / shuffle"). Each role's lifetime, holds, and never-holds is given.
2. **Load-bearing rule.** PASS. Stated verbatim as a blockquote (line 25: *"The driver plans and talks; all work — however small — is a worker dispatch."*) with all sub-rules: driver never edits a work artifact (line 29), read-and-summarize dispatch for inspection (line 31), single writer of the store (line 33).
3. **Non-monotonicity guarantee per role + honest driver limit.** PASS — see CHECK 3 for the decision-fidelity detail. Workers (line 41), store hot path (line 43), driver checkpointed-not-stateless with the honest limit (lines 45–49).
4. **Canonical net principle.** PASS. Closes on the exact sentence as a blockquote (line 55: *"No context on the hot path is long-lived; the one long-lived context carries the index, not the work."*), matching oracle §4 net principle verbatim.

**Three-role table present.** PASS — lines 9–13, columns Role / Maps to (Spark) / Lifetime / Holds / Never holds, faithfully mirroring oracle §3's table (lines 26–30).

**Length band (~900–1300).** PASS. 1273 prose words (excl. the table); 1408 incl. table. Within band on the prose measure; only marginally over if the table cells are counted as prose, which is not the natural reading. No finding.

**Forward link to ch3.** PASS. Final paragraph (line 57): *"The next chapter makes the driver's plan concrete: work as a DAG — units as self-contained dispatch specs, the ledger as the DAG and scheduler state, and foundation as topology."* — matches oracle §8 ch3 scope.

## CHECK 3 — Decision fidelity (CRITICAL)

All decision-critical claims match the oracle exactly. No violations.

- **Re-hydration = store + session-start reconcile, NO compaction-hook dependence.** PASS. Line 47: *"re-hydration is runtime-agnostic and depends on no compaction hook. An optional hook could auto-inject the manifest on a compaction event, but it is at most an accelerator; the framework deliberately does not build on it, because the store-plus-reconcile path works on every runtime without it."* This is a faithful restatement of oracle §4 / Q-v3-2 ("STORE + SESSION-START RECONCILE (no compaction hook)"). The `session-start reconcile` term is named (lines 47, 49).
- **No stateless interactive driver claimed/implied.** PASS. Line 45: *"In interactive Claude Code the driver is the session. It cannot be made stateless..."* and line 49: *"never sell a stateless interactive driver... re-hydration, not statelessness, is what makes it survivable."* Directly affirms the limit; never implies statelessness.
- **GOTM cannot self-trigger /compact interactively.** PASS. Line 45: *"it cannot self-trigger `/compact`: compaction is human-only, no hook or model directive fires it, and the auto-compact threshold is not tunable."* Matches oracle §4's verified parenthetical exactly.
- **SDK self-compact framed as a bonus, not a dependency.** PASS. Line 49: *"In SDK or headless mode the driver gains one extra capability — it can compact itself programmatically. That is a bonus where the runtime allows it, not a requirement the architecture leans on."* Matches oracle §4.
- **Single-writer rule present.** PASS. Line 33: *"The driver is the single writer of the store... There is exactly one hand that writes the ledger and the durable state... Many contexts read the store; one writes it."*
- **"Driver never edits work artifacts" present.** PASS. Line 29: *"The driver never edits a work artifact. Not a chapter, not a config file, not a line of code... a one-line fix is still a worker."*

## CHECK 4 — Canonical terms

All required terms present and used correctly:

- driver / worker / store — throughout. PASS.
- hot path — lines 13, 19, 31, 43, 53, 55. PASS.
- monotonic / non-monotonic — lines 3, 29, 37, 39, 41, 43 (and section heading line 37). PASS.
- context economy — present in spirit via "context economy discipline" framing; the explicit phrase appears as the concept ("frugal everywhere there are many contexts," line 35) but the literal two-word term "context economy" is not used in ch2. MINOR — see findings (the term is established in ch1 and the concept is clearly carried; not load-bearing here).
- durable store — line 19 (*"the durable store, the system of record"*). PASS.
- transcript independence — line 47 (*"the same transcript-independence guarantee GOTM has always made"*). PASS.
- checkpointed — table line 11, lines 45, 47, 53. PASS.
- session-start reconcile — lines 47, 49. PASS.
- unit — throughout (lines 12, 15, 17, 29, 41, etc.). PASS.
- ledger — lines 11, 33, 43. PASS.

## CHECK 5 — Internal consistency, quality, continuity with ch1

PASS. The chapter opens by explicitly picking up ch1's closing law (line 3: *"The previous chapter ended with a single law — nothing on the hot path is long-lived — and a promise that everything else is a consequence of it"*) — clean continuity with ch1's line 39 forward link. Internally consistent: the table, the prose role descriptions, the load-bearing rule, and the per-role guarantee all reinforce the same model with no contradictions. Prose is precise and free of hype; claims are hedged honestly (the "honest limit" section is notably candid). The numeric/observed claims from ch1 (987K, etc.) are not re-asserted here, avoiding redundancy. No quality concerns.

## CHECK 6 — No encroachment

PASS. The chapter introduces but does not develop later-chapter material:
- **ch4 (loop):** mentions "the scheduler loop" as a thing the driver holds (table line 11, line 15) but does not describe ready-set/dispatch/collect/retry mechanics. Introduces only.
- **ch5 (fan-out / economy):** names "parallel fan-out" once in the net-principle paragraph (line 53) as a downstream consequence; does not explain fan-in/barriers/worker minimalism. Introduces only.
- **ch3 (ledger / DAG depth):** references "the plan (the ledger DAG)" and "born tiered" (table line 11, lines 15, 43) but defers the DAG/unit/foundation treatment to ch3 via the explicit forward link (line 57). Introduces only.

No section over-reaches into another chapter's deliverable scope.

---

## Findings

**HIGH:** none.

**MEDIUM:** none.

**LOW:**
- L1 (canonical terms / cosmetic): the literal two-word phrase **"context economy"** does not appear in ch2, though the concept is carried explicitly ("frugal everywhere there are many contexts," line 35) and the term is established in ch1. Not load-bearing for this chapter; cosmetic only.

---

**Summary:** HIGH: 0 · MEDIUM: 0 · LOW: 1 · Verdict: PASS-FINDINGS
