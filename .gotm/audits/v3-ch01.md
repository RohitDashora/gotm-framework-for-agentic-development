# Audit — v3 Chapter 1: The problem & the thesis

**Target:** `docs/01-the-problem-and-thesis.md`
**Oracle:** `V3-DESIGN.md` (locked blueprint)
**Auditor:** independent (not the author)
**Date:** 2026-06-26

---

## 1. Existence

File exists at the path with real, substantive content: 39 lines, 1043 words, four section headers under a real H1 title. PASS.

## 2. Spec match

All four required elements present:

- **(1) Problem established** — bounded-context regime + context as scarce/lossy resource. Cited: *"We call this regime **bounded-context agentic execution**: real work, much larger than one context…"* and *"**context is the scarce, lossy, expensive resource.**"*
- **(2) Failure named = monotonicity** — both sub-failures present. Ledger: *"The single durable artifact only grew, and was re-read every turn."* Doer: *"One long-lived agent accumulated all execution state… it **self-certified its own work**… a real, observed death at roughly **987,000 tokens**."* Root cause: *"conflated the planner and the doer in one ever-growing context… **no theory of ephemerality**."*
- **(3) Thesis pull-quote** — rendered verbatim as a blockquote (see Finding L-1 on a one-word variance).
- **(4) Preview as consequences w/o mechanics** — *"This chapter argues a thesis; it does not yet build the machine… Each is a **consequence** of the single sentence above."*

Length 1043 words — within the ~700–1100 band. Real `# Title` ("The problem & the thesis"), not a "Chapter 1:" stub. Ends with a forward link to ch2: *"The next chapter builds the first and most important consequence: the architecture — **driver, worker, and store**…"*. PASS.

## 3. Decision fidelity

- ~987K stall: chapter says *"roughly **987,000 tokens**"*; blueprint §1 says *"(~987K tokens, observed)"*. Match.
- Self-certification: chapter *"the agent **self-certified its own work**, which is no audit at all"*; blueprint §1 *"self-certified its own work (audit independence eroded)"*. Match.
- Ledger numbers: chapter *"a 380 KB ledger, roughly ninety-five thousand tokens… six hundred to two thousand tokens"*; blueprint §1 *"a 380 KB / ~95K-token ledger… ~600–2,000 token cell"*. Match.
- Root cause as one thing (no ephemerality + planner/doer conflated): faithful to blueprint §1.
- Frames/motivates only — does not explain driver/worker mechanics in any operational depth (see §6). PASS.

## 4. Canonical terms

`monotonic`/monotonicity, `context economy`/context-economy, `durable store`, `transcript independence`, `hot path`, `unit`, `ledger`, `store`, `driver`, `worker` all used and used correctly. Notably `transcript independence` is correctly tied to the founding invariant (*"any context can be thrown away and rebuilt from disk"*), and `hot path` is correctly defined (*"everything read or run on a recurring basis"*). PASS.

## 5. Internal consistency + quality

Declarative, concrete, no hype/filler. Strong opening: problem → failure → thesis → consequences arc mirrors the blueprint's own structure. No internal contradictions found. The "born tiered" claim and the index-not-work driver framing are consistent with blueprint §4/§5. PASS.

## 6. No encroachment

The "Everything that follows is a consequence" section names driver/worker/store and audit-independence but does so as *implications* ("the machine is already implied"), explicitly deferring construction to ch2 (*"it does not yet build the machine"*). This motivates rather than specifies — no roles table, no lifetimes, no scheduler loop, no honest-limit treatment. Within spec. PASS. (See M-1 for the one phrase that leans closest to the line.)

---

## Findings

### HIGH
None.

### MEDIUM

**M-1 — Consequences section states mechanism conclusions, not just motivation (borderline encroachment).**
The preview asserts derived results with some specificity: *"Separate the planner from the doer and you get a long-lived coordinator and short-lived executors — a **driver** and **workers** — talking only through the **store**."* and *"Make the durable record born-tiered and you get a cheap hot path with an unbounded but never-read cold archive."* These are ch2/ch3 conclusions stated as outcomes. It stays on the right side of the line (no how, no roles table, no loop), and the spec does say "preview the rest as consequences," so this is non-blocking — but it is the section a reader could mistake for ch2's payload. Worth a glance on the ch2 handoff to ensure no thunder is stolen.

### LOW

**L-1 — Thesis pull-quote has a one-word variance from the canonical string.**
Spec/blueprint §2: *"every working context is disposable and reconstructable from it."* Chapter line 27: *"every working context is disposable and reconstructable from it."* — these match. Verified the full blockquote is reproduced verbatim including *"Nothing on the hot path is long-lived."* No deviation found on re-check; logging as LOW/no-op for traceability. (No change required.)

**L-2 — Title casing is sentence-case ("The problem & the thesis").**
Matches the blueprint deliverable-map label (§8.1) exactly, including the ampersand. Cosmetic only; flag for house-style consistency across chapters if a later style pass standardizes capitalization. No action required.

---

## Summary
HIGH: 0 · MEDIUM: 1 · LOW: 2 · Verdict: PASS-FINDINGS
