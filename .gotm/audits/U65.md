# Audit report — spec-match / 7-point checklist — 2026-06-23

## Target audited
File: `templates/PROTOCOL.md.template`
Unit: U65

Oracle: `.gotm/DECISIONS.md` → D22; `geniefy-v3/docs/GOTM-FEEDBACK.md` lines 119–137 (G15/G17/G18); `prompts/audit.md`.

U65 promised three edits to this one file, per D22: **G15** (Resilience — one recovery log, one ordering + a second-log reconcile lint), **G17** (Audit gates — expand the default checklist 5→7 with an enforcement check and a multi-site-claim check), **G18** (session-start reconcile — a ledger-parse lint). Audited mechanically as a fresh context (auditor ≠ author). The working-tree diff against `HEAD` isolates exactly these changes (the U64/D21 lines were already committed at `HEAD`); the three edits are additive, no prior content removed except the `5`→`7` count in the checklist header.

## Findings

### HIGH severity
- None.

### MEDIUM severity
- None.

### LOW severity
- **(cosmetic) Source finding G17 named "Appendix D", the edit lands in *Audit gates*.** `GOTM-FEEDBACK.md` line 132 recommends adding the two checks "to the audit checklist in `PROTOCOL.md.template` (Appendix D)." This template has no Appendix D; the checklist lives in the *Audit gates* section (line 69), which is where the prior 5-point list already lived and where D22 itself directs the edit ("`templates/PROTOCOL.md.template` (Audit gates)"). So the placement is correct and matches D22 — the only mismatch is against the source finding's stale section name, which D22 already reconciled. No action needed; noted for completeness.

### UNVERIFIED
- **LEDGER-template side of G15 (out of scope here).** D22's G15 is a two-site change: `templates/PROTOCOL.md.template` (this unit, U65) **and** `templates/LEDGER.md.template` (U66). Per the audit brief, only the PROTOCOL side is in scope here; the LEDGER side is U66's audit. The PROTOCOL side is internally complete on its own (see check 7 below). Not a finding against U65 — flagged only to record that the multi-site claim's other half is verified elsewhere.

## Checklist results (7-point)

1. **Existence** — PASS. `templates/PROTOCOL.md.template` exists at the ledger's stated path (183 lines, read in full).

2. **Spec match** — PASS. All three promised changes are present and say what D22 promised:
   - **G15** — new paragraph "**One recovery log, one ordering.**" (line 117): pins `Recent updates` as *the* recovery log, **newest-first**; states `## Active unit` "holds **only a pointer to the current unit** — never a growing stack of dated status banners"; names the second-de-facto-log failure mode; and points to the reconcile lint. The matching **Second-recovery-log lint** bullet is added to session-start reconciliation (line 138): warns when >1 dated status entry accumulates under `## Active unit` while `Recent updates` is also written. All three sub-claims of D22's G15 (single log / pointer-only / reconcile-warn) are covered.
   - **G17** — checklist header changed "default **5**-point" → "default **7**-point" and "runs all **five**" → "runs all **seven**" (line 69); adds (6) **enforcement check** (behavioral decision held by gate/config/assertion, not only prose; documented-but-unenforced is a finding) and (7) **multi-site claim check** ("wired into both / applied across N / replaced everywhere" verified by grep/count, guard per site). Matches D22's 5→7 wording verbatim in intent.
   - **G18** — **Ledger-parse lint** bullet added to session-start reconciliation (line 139): every `| U<n> |` row splits to the expected columns with a status ∈ {pending, in_progress, done, superseded} and a parseable Output cell; calls out that the immutability hook parses the same table positionally. Matches D22's G18.

3. **Cross-reference integrity** — PASS. Every internal reference the new text adds resolves to a real section in the same file:
   - "(see below)" in the *One recovery log* paragraph → the Second-recovery-log lint bullet that follows in session-start reconciliation. ✓
   - "See *One recovery log, one ordering* above." in the Second-recovery-log lint → the heading at line 117. ✓
   - "see the enforcement note above" in the Ledger-parse lint → the "**Mechanical enforcement is optional and lives in tooling.**" note at line 109 (Anti-drift safeguards), which is above the Resilience section where the lint sits, so "above" is accurate. ✓
   - The checklist's parenthetical ("Checks 6–7 were added after the only two FAILs…") is narrative justification, not a `D<n>`/`U<n>`/`Q<n>` citation — nothing to resolve.

4. **Internal consistency** — PASS. No contradiction with the rest of the protocol:
   - The new *One recovery log* paragraph reinforces, rather than contradicts, the immediately-preceding **transcript-independence** invariant (line 115), which already says "Treat `LEDGER.md` → Recent updates as the recovery log." Both now agree `Recent updates` is the single log.
   - The two new lints (Second-recovery-log, Ledger-parse) extend the existing session-start reconciliation bullet list (alongside Audit-gate and Stale-by-decision lints) without altering or conflicting with the existing bullets.
   - The 7-point checklist in *Audit gates* is consistent with the unchanged verdict paragraph and the cadence invariants below it.

5. **Decision fidelity** — PASS. The output honors D22's three bullets for G15/G17/G18 point-by-point (the "framework half now" scope). It does not stray into G16 (consume side, U68–U70) or the plugin-runtime items D22 defers to v2.6.0.

6. **Enforcement check** — PASS. Per the brief: G18's lint text correctly **defers the runtime lint to adopter tooling** ("the actual lint belongs in adopter tooling — see the enforcement note above") while **keeping the manual check** ("Check every `| U<n> |` row splits…"). This is the protocol's stated D12/D13 stance (paste-able discipline; runtime enforcement lives in adopter tooling), so the absence of a shipped runtime gate here is by design, not a documented-but-unenforced gap. The G17 checklist additions (6)/(7) are themselves framed as checks an **auditor performs**, not as runtime assertions — appropriate for this framework.

7. **Multi-site claim check** — PASS (PROTOCOL side). D22's G15 spans `templates/PROTOCOL.md.template` **and** `templates/LEDGER.md.template`. Scope-limited to the PROTOCOL side here: it is **internally complete** — the *One recovery log* heading and the Second-recovery-log reconcile lint are both present and mutually cross-referenced, so the PROTOCOL half stands on its own. The LEDGER half is U66's audit (recorded under UNVERIFIED above, not a defect of U65).

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 · UNVERIFIED: 1 (out-of-scope sibling, not a U65 defect)
Verdict: PASS-FINDINGS   (HIGH ⇒ FAIL; MEDIUM/LOW-only ⇒ PASS-FINDINGS; clean ⇒ PASS)
