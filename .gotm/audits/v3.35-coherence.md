# v3.35 coherence + validation audit (independent)

**Verdict: PASS-FINDINGS.** All 5 enhancements (E1–E5) landed consistently
across BOTH repos, differing only by neutral-vs-`.gotm/` paths as designed. One
small cross-file inconsistency fixed inline; one out-of-scope decision (D5
version bump) flagged, not fixed.

Scope: independent verification of the v3.35 change-set against
`V3.1-ENHANCEMENT-DESIGN.md §4` (LOCKED). Files reviewed: both PROTOCOL +
LEDGER templates, both `audit.md`/`worker-dispatch.md`/`driver-loop.md` prompts,
framework `docs/03–06`, plugin `commands/{audit,bootstrap}.md`, the immutability
hook, and `compact_ledger.py`. Author was already gone (structural independence).

---

## 1. Kind vocabulary is ONE set everywhere — PASS (after fix)  [W3's cross-file item]

Canonical set (D3): `authoring · ui · eval · deploy-infra · data · diagnosis`,
default `authoring`. Verified identical across every surface that enumerates it:

| Surface | Kind vocab |
|---|---|
| framework LEDGER template Conventions | all 6, each with its verified-done dimension |
| plugin LEDGER template Conventions | all 6, identical |
| framework PROTOCOL kind→dimensions table | all 6 |
| plugin PROTOCOL kind→dimensions table | all 6 |
| framework `prompts/audit.md` §4b | **FIXED** — now lists all 6 inline |
| plugin `prompts/audit.md` §4b | all 6 inline |
| plugin `/gotm:audit` command | all 6 inline |
| both `driver-loop.md` | 5 runtime kinds (`ui/eval/deploy-infra/data/diagnosis`) — correct: `authoring` needs no runtime dimension |

**Fixed inline:** framework `prompts/audit.md:82` §4b read only "Read the unit's
**Kind** (ledger column; default `authoring`)" — it omitted the slug list that
its plugin sibling, the `/gotm:audit` command, and both LEDGER templates all
carry. Edited to match the plugin's phrasing exactly:
`(the ledger's `Kind` column; default `authoring`, kinds: `authoring` · `ui` ·
`eval` · `deploy-infra` · `data` · `diagnosis`)`. Now the auditor reads the same
6-slug set no matter which surface it lands on.

No variant/stale slugs anywhere (`deploy/infra` as a slug, `visual`,
`measurement`, `render` as kinds — none found). The header-aware hook reads the
`Kind` column by name, so the extra column doesn't shift the Output/Status parse.

## 2. E1 Output-cell contract — PASS

Stated identically in LEDGER templates (Conventions "OUTPUT CELL = ownership
key"), `worker-dispatch.md` §4, PROTOCOL "ledger micro-schema", and docs/03
("Output cell is a machine-authoritative key"): concrete backticked path(s) only,
no globs/dirs, no raw `|`, multi-file = comma-separated backticks, hook parses
ALL backticks. **Ledger-parse lint** has one stated home consistently: "at
session-start reconcile AND on every driver write" — present in both PROTOCOLs,
driver-loop turn-end reconcile (step 7), docs/03, and both LEDGER Conventions.
Hook confirmed: `_strip_output` does `re.findall` (all paths); `_parse_table_line`
fails safe on a mis-split row (skips, freezes nothing, stderr note).

## 3. E3 destructive-op gate — PASS

Same D4 op set (deploy/apply-that-destroys, `DROP`, `delete`, `unbind`/`rebind`,
`REVOKE`, force-push, truncate/purge/overwrite-in-place), the "remove-from-IaC +
apply = DELETE / decouple with `unbind`, never delete-from-config" trap, and the
pre-execution independent plan-review — all present in both PROTOCOLs
(§Destructive-op gate), both driver-loops (step 2, pre-gate), docs/04 (§2), and
plugin bootstrap.md (rule 9). Neutral-vs-plugin split correct: framework uses
generic examples (`terraform apply`, IaC deploy); plugin/bootstrap use concrete
`databricks bundle deploy`. docs/04 carries the trap in prose with an
abbreviated op list — appropriate for a narrative chapter.

## 4. E4 detail-to-disk — PASS

≤ ~8-line return + full-detail-to-file + over-cap = defect stated consistently in
both `worker-dispatch.md`, both PROTOCOLs (worker contract), both driver-loops
(step 4), and docs/05 (§ token economy). Paste-ready dispatch template in both
worker-dispatch.md states the cap.

## 5. E5 proof-stamped DISPATCHED — PASS

"Stamp `in_progress`/DISPATCHED only after the Agent launch returns an id (id =
proof); turn-end rescan that no such row lacks a real dispatch" — consistent in
both driver-loops (step 3 + Do-not list), both PROTOCOLs (loop step 2 + turn-end
reconcile step 7), and docs/04 (§2 "DISPATCHED means launched, and the driver
proves it"). Neutral split correct: framework says "Agent call"; plugin says
"`Agent` launch returns an agentId". Compaction E5/B1 (append + back-up-first
`LEDGER-ARCHIVE.md`) present in `compact_ledger.py`; py_compile clean.

## 6. Framework ↔ plugin parity — PASS

Both repos carry all 5 enhancements; the only differences are the intended
neutral-vs-`.gotm/` path bindings and concrete-vs-generic op examples. All plugin
path references resolve: bootstrap.md's referenced templates
(`session-start.md`, `consult.md`, `outcome-analysis.md`, `hooks/README.md`, all
`*.template` files, `scripts/compact_ledger.py`) all exist under
`gotm/templates/`. No dangling refs. `/gotm:audit` and `/gotm:loop` and
`/gotm:learn` cross-references all point at real commands/prompts.

## 7. Terminology + cross-refs — PASS

No contradictions found between PROTOCOL and the prompts it points to
(loop steps, worker contract, audit gates, verified-done kinds all align).
authored-done vs verified-done, the 3 verdicts, the gate, freeze + follow-on
ownership — all stated identically across PROTOCOL / docs / prompts / hook.

---

## Findings

### MEDIUM (non-blocking, flagged — outside the named change-set)

- **D5 version bump NOT applied.** D5 (LOCKED) requires `plugin.json` +
  `marketplace.json` → `3.35.0`. Actual: `gotm/.claude-plugin/plugin.json` =
  `3.0.0`; the experimental `marketplace.json` gotm entry (line 3011) = `3.0.0`.
  Neither file is in the stated v3.35 change-set, and the WORKER report
  (`v3.35-fix-machinery.md`) covered only 4 files — so this is a pending
  delivery step, not a regression. It should be applied before the v3.35 tag is
  cut (and the two must stay in sync). Not fixed inline: it touches a published
  marketplace outside this coherence scope; surfacing to the driver.

### Fixed inline
- framework `prompts/audit.md` §4b — added the full 6-slug Kind vocabulary to
  match every other surface (item 1).

### Nits (LOW, not acted on)
- `__pycache__/*.pyc` committed under `gotm/templates/{hooks,scripts}/` — stray
  build artifacts; harmless, not shipped-behavior.
