# Audit — Chapter 7: Resilience & the three-tier memory economy

**Target:** `docs/07-resilience-and-memory.md`
**Oracle:** `V3-DESIGN.md` (§4 honest limit, §5 memory/resilience rows, §6 step 6, §10 Q-v3-2)
**Continuity context:** ch4 (loop / session-start reconcile), ch6 (verified-done gate, freeze), ch8 (forward link)
**Auditor:** independent (did not author the chapter)
**Date:** 2026-06-26

---

## Tier 1 — Existence

PASS. The target exists, is non-empty, and is a coherent finished chapter. Title is real and descriptive: `# Resilience & the three-tier memory economy` (L1). Five `##` sections plus an opening framing: crash model (L7), three tiers (L19), compaction (L31), the invariant restated (L41).

## Tier 2 — Spec match (all five elements + length + title + forward link)

PASS on all five required elements.

1. **Two-level crash model** — Covered, §"The crash model has exactly two levels" (L7–17). Worker crash framed as retry / idempotent lineage recompute: "**A worker crash is a task retry.**" (L11), "the inputs still live on disk, untouched, so re-running the unit is a safe **lineage** recompute" (L11), "the same dispatch the scheduler always does" (L11), idempotence explicit (L11). Driver crash as re-hydrate: "**A driver crash is a re-hydrate.**" (L13), rebuilt from the store via session-start reconcile (L15). Both levels present and correctly attributed.

2. **The honest limit (CRITICAL)** — Covered and faithful, L13–17. Interactive driver cannot self-trigger compaction: "it cannot be made stateless and **it cannot self-trigger `/compact`** (compaction is human-only; no hook or model directive fires it; the auto-compact threshold is not tunable)" (L13). Runtime-agnostic re-hydration depending on no compaction hook: "**re-hydration is runtime-agnostic and depends on no compaction hook.** It works on every runtime because it leans on nothing but the store and the reconcile" (L17). Optional SessionStart hook framed as accelerator only: "it is at most an accelerator, and the framework deliberately does not build on it" (L17). SDK self-compaction as a bonus: "In SDK or headless mode the driver gains one more capability — it can additionally compact *itself* programmatically — but that too is a bonus ... never a dependency" (L17). "No context loss across accidental/hard ends" framing present: "the promise of 'no context loss across any session end' holds for the *accidental and hard* endings — the crash, the killed terminal, the surprise compaction" (L17).

3. **Three tiers** — Covered, §"Three tiers, each reconstructing the one above it" (L19–29) with table (L23–27). T1 conversation (L25), T2 hot durable / frontier (L26), T3 cold durable / archive + `audits/` + `DECISIONS.md` + `docs/` (L27). Cheaper-but-larger relationship and each tier reconstructing the hotter one: "each cheaper-but-larger than the last, each able to reconstruct the hotter one" (L21); "T1 is reconstructed from T2 ... T2 is reconstructed from T3" (L29).

4. **Compaction** — Covered, §"Compaction is the anti-monotonicity mechanism" (L31–39). Done+audited cell → one-line archive entry keeping the audit pointer (L35). Lossless: "**lossless** because every fact it removes from the hot tier still exists, in full, one pointer away in T3" (L35). Gate-preserving: "**gate-preserving**: the audit pointer travels with the archived cell, so the verified-done gate (chapter 6) stays provable forever" (L35). Index op not a frozen-output edit, does not violate the freeze: "compaction is **an index operation, not an edit to a frozen output** ... it does not violate the freeze: the freeze protects artifacts, and the ledger's frontier is not an artifact" (L37). Triggered at session-start reconcile on a size/count threshold (L37). Rolling recovery-log window (L37).

5. **Transcript independence as the invariant** — Covered, stated up front (L5) and as the closing §"The invariant, restated" (L41–43): "**transcript independence** — the property that on-disk state alone reconstructs working context" (L43); all three resilience moves tied back to it (L43).

**Title:** Real and on-spec (matches §8 ch7 entry). **Forward link to ch8:** Present and correct (L45) — "The next chapter, *In practice* ... how the interactive and SDK drivers differ, how to bootstrap a v3 project, and how this very rewrite is its own worked example." Matches §8 ch8.

**Length (LOW):** 1543 raw words; 1457 prose words excluding the small table. Spec band is ~900–1300 with ~150w over acceptable (~1450 ceiling). Prose-only sits ~7 words over even the lenient ceiling — effectively at the boundary. The blueprint itself pre-flags this (§11 coherence-pass note: "ch7 ~150w over"). Trim candidates exist in the dense crash-model paragraph (L11). Noted, not blocking.

## Tier 3 — Decision fidelity (CRITICAL → any violation = HIGH)

PASS. No violations. Every CRITICAL gate is met.

- **Re-hydration depends on NO compaction hook (store + session-start reconcile):** Faithful, stated as the load-bearing property — "**re-hydration is runtime-agnostic and depends on no compaction hook.** It works on every runtime because it leans on nothing but the store and the reconcile" (L17). Matches §4 and Q-v3-2 verbatim in substance ("STORE + SESSION-START RECONCILE (no compaction hook)").
- **Must NOT claim interactive self-`/compact`:** Compliant — chapter states the opposite explicitly: "**it cannot self-trigger `/compact`**" (L13). Self-compaction is correctly confined to SDK/headless as a bonus (L17). No leak.
- **Must NOT claim a stateless driver:** Compliant — "it cannot be made stateless" (L13); driver is "**checkpointed**, not stateless" (L15, L25). The framing "We promise one that always comes back" (L13) is the §4 stance, not a stateless claim.
- **Compaction framed as an index op that does NOT violate the freeze:** Faithful (L37) — rewrites a ledger cell, never the artifact; "the freeze protects artifacts, and the ledger's frontier is not an artifact — it is the live index of the work." Consistent with §5 freeze row and ch6's freeze.
- **Worker-retry/lineage + driver-re-hydrate both present:** Both present and correctly mapped (L11, L13–17), matching §5 resilience row and §6 step 5/6.

Optional-accelerator handling of the `SessionStart` hook (L17) matches Q-v3-2's "we do not depend on a SessionStart(compact) hook ... at most an accelerator."

## Tier 4 — Canonical terms

PASS. All required terms present and correctly used:
retry (L11), lineage (L11, L43), re-hydrate / re-hydration (L13, L15, L17, L29, L43), session-start reconcile (L15, L37), checkpointed (L13, L15, L25), T1/T2/T3 (L25–29, L37), frontier (L15, L26, L29, L37), archive (L27, L29, L35), born tiered (L26, L39), compaction (L31+), lossless (L35), transcript independence (L5, L43). Also: store, hot path, audit pointer, recovery-log window, gate-preserving — all used consistently with the blueprint.

## Tier 5 — Internal consistency, quality, continuity

PASS. No HIGH/MEDIUM issues.

- **Internal consistency:** The "two crash cases only" claim (L9) is justified by the two-context architecture (worker, driver) and both are then handled — closed reasoning. The tier table (L23–27) and the prose downward-reconstruction (L29) agree. Compaction's "lossless" claim (L35) is internally grounded by the audit-pointer-travels argument and is consistent with the §5 "Memory" three-tier row.
- **Quality:** High. The "conversation is a cache; the store is the truth" line (L5) and the closing "same single bet" (L43) give the chapter a tight thesis-to-restatement arc. The honest-limit paragraph (L13–17) is precise and avoids overselling — exactly the §4 tone.
- **Continuity:** Backward references are accurate and load-bearing, not decorative — ch1 boundary (L3), worker-as-function-of-inputs ch2 (L11), single-writer ch2 (L11), loop step 5 ch4 (L11), session-start reconcile ch4 (L15), verified-done gate ch6 (L35), born-tiered ch3 (L39). Forward handoff to ch8 is clean (L45).

## Tier 6 — No encroachment (ch4 / ch5 / ch6 beyond the gate reference)

PASS.

- **ch4 (loop):** References the reconcile and loop step 5 as named prior mechanics (L11, L15) without re-teaching the ready-set/dispatch loop. Legitimate citation, not encroachment.
- **ch5 (scaling/economy):** Not re-explained. "anti-monotonicity" framing (L31) is resilience-scoped (compaction keeping T2 small), not a fan-out/economy lecture.
- **ch6 (audit/freeze):** Only the gate and freeze are *referenced* ("the verified-done gate (chapter 6) stays provable", L35; freeze protects artifacts, L37) — exactly the permitted gate reference. Structural audit independence / verified-done mechanics are not re-derived.

---

## Summary

`HIGH: 0 · MEDIUM: 0 · LOW: 1 · Verdict: PASS-FINDINGS`

LOW-1: Length runs to ~1457 prose words (1543 raw) — at/just past the lenient ~1450 ceiling (1300 + 150w). Pre-flagged in the blueprint's coherence-pass note (§11). Trim candidates in the dense crash-model paragraph (L11). Cosmetic; not blocking.
