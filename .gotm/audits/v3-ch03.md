# Audit — Chapter 3: Work as a DAG

**Target:** `docs/03-work-as-a-dag.md`
**Oracle:** `V3-DESIGN.md` (§5 primitives table, §3, §6)
**Continuity context:** `docs/02-driver-worker-store.md`
**Auditor:** independent (did not author the chapter)
**Date:** 2026-06-26

---

## Tier 1 — Existence

PASS. The target file exists, is non-empty (1257 raw words; 1105 prose words excluding tables/code), titled `# Work as a DAG: units, the ledger, foundation`, and is a coherent, finished chapter.

## Tier 2 — Spec match (four required elements + length + forward link)

PASS on all required elements.

1. **Unit = self-contained worker dispatch spec** — Covered. §"The unit is a self-contained worker dispatch spec" (L5–18) names all four payload elements verbatim: "**Bounded inputs**" (L11), "**Exactly one output**" (L12), "**A spec**" (L13), "**Constraints**" (L14). Fresh-worker-without-conversation test stated: "*a fresh, stateless worker could execute it from the dispatch alone, with no access to the conversation that created it.*" (L16). Amortized batching covered (L18): "**amortized batching**: group the trivia into one **partition-worker**", "the driver never edits a work artifact" (L18), "all work is a worker dispatch" (L18).

2. **Ledger = DAG + scheduler state, born tiered** — Covered. §"The ledger is the DAG plus the scheduler's state" (L20–42). Carries "the *topology* ... and the *scheduler state*" (L22). Old flat monotonic ledger contrasted: "a sharp break from v2, where the ledger was a flat, append-only log, re-read whole on every turn ... that design was **monotonic**" (L24). "**born tiered** — split into two tables from the first unit, not compacted as an afterthought" (L24). Frontier/archive table present (L26–31). Driver-only-writer present (L42).

3. **Foundation = DAG topology** — Covered. §"Foundation is just DAG topology" (L44–48): foundation = "the *upstream nodes* of the DAG" (L48); foundation-before-drafts as graph property: "no longer something to remember and enforce; it is what a correct topological walk of the DAG *does*" (L48).

4. **One-paragraph preview of authored-done vs verified-done, deferring to ch6** — Covered. §"Two done states — a preview" (L50–52) is a single paragraph; explicitly defers: "the full treatment of structural audit independence is the subject of chapter 6" (L52).

**Length:** 1105 prose words (tables/code excluded) — inside the 800–1200 band; 1257 with tables. Within spec.

**Forward link to ch4:** Present and correct. L3 ("the dynamics ... are the next chapter"), L48 ("next chapter"), and the closing forward link L56: "**the loop: the driver's scheduler**, walking the DAG to compute the ready set, dispatch workers, and collect results." Matches ch4 per §8 deliverable map.

## Tier 3 — Decision fidelity (vs blueprint)

PASS. All six tested decisions are faithfully rendered, no contradictions with §5/§3/§6.

- **Unit-as-dispatch-spec** (§5 row "Atomic unit"): faithful — "bounded inputs + one output + spec + constraints, complete without the conversation. The dispatch contract is the center of gravity" → chapter L7, L16 ("**The dispatch contract is the center of gravity**"). Exact.
- **Born-tiered ledger, frontier/archive** (§5 "Ledger", §3 store row): faithful — frontier "ready + active units and their immediate inputs/pointers" (L28) matches §3 "hot frontier (active units + recovery window)"; archive "closed units — terse pointer only" (L29). Hot-path-only-frontier (L31) matches §4/§6b.
- **Driver-only-writer kills dup-row race** (§5 "Anti-drift/freeze", §3 load-bearing rule): faithful — "**only the driver writes it.**" (L42), "Single-writer discipline kills that race by construction" (L42), explicit v2 dup-row contrast.
- **Foundation-as-topology** (§5 "Foundation gate"): faithful — L48 matches "the scheduler respects deps natively; foundation units are upstream nodes."
- **authored-done/verified-done only PREVIEWED, not fully explained (ch6 owns it)** (§5 "Audit cycle", §8 ch6): faithful and correctly bounded. Chapter previews the distinction and the never-self-certify point (L52) but explicitly defers full structural-independence treatment to ch6. Does not encroach.
- **Amortized batching / always-dispatch / driver-never-edits** (§5 row, §6b, Q-v3-1): faithful — L18 matches "Always dispatch; batch trivia into one partition-worker; the driver never edits a work artifact" verbatim in substance.

## Tier 4 — Canonical terms

PASS. All required canonical terms present and used correctly:
driver (L3, L22, L42, L48, L56), worker (L7, L9, L42, L52), store (implicit via roles; "the store's hot tier" concept carried — see note below), unit (L5+), ledger (L20+), DAG (L3, L22, L48, L56), frontier (L24–31), archive (L29–31), born tiered (L24, "**born tiered**"), foundation (L44–48), hot path (L31, "the only part on the **hot path**"), authored-done (L38, L52), verified-done (L52).

**LOW note:** The bare noun "store" as a standalone role-word appears less prominently in ch3 than in ch2 — ch3 references the durable files by name (`audits/`, `DECISIONS.md`, `docs/`) and "off the hot path" rather than repeatedly invoking "**store**." This is acceptable (ch3 is unit/ledger-scoped, not role-scoped) and not drift, but noted for the coherence pass.

## Tier 5 — Internal consistency, quality, terminology-drift vs ch2

PASS. No HIGH/MEDIUM drift found.

- **Opening continuity:** L3 explicitly recaps ch2's three roles and law ("named the three roles — **driver**, **worker**, **store** — and the law ... nothing on the hot path is long-lived"), and ch2's closing forward link (L57) promises exactly what ch3 delivers. Clean handoff in both directions.
- **driver/worker/store usage consistent with ch2:** Same definitions — driver = conversation agent / single writer / plans-and-talks; worker = ephemeral, bounded inputs + one output + structured result; frontier/archive tiering identical. No contradictory redefinition.
- **One minor wording variance (LOW):** ch2 standardizes the worker return as "**terse structured result**" (ch2 L12, L17); ch3 says workers "**return a structured result**" (L42) — drops "terse." Same concept, not a contradiction. Cosmetic only.
- **"center of gravity" framing (L7, L16)** vs ch2's "load-bearing rule" — complementary, not conflicting; the blueprint itself uses "the dispatch contract is the center of gravity" (§5), so ch3 is faithful to the oracle's own phrasing.
- Quality is high: the frontier/archive table and the example ledger sketch (L36–40) carry weight efficiently; the `authored-done` status appearing in the sketch and then being explained in the preview paragraph is a nice internal callback.

## Tier 6 — No encroachment

PASS. The chapter stays in its lane.

- **ch4 (loop mechanics):** Deferred, not done — "the dynamics ... are the next chapter" (L3), "The scheduler respects dependencies natively (next chapter)" (L48). Mentions ready/active/done/blocked statuses as ledger *state* (legitimate static structure) without describing the ready-set computation or dispatch loop.
- **ch5 (fan-out/economy):** Only pointed to, not executed — "A unit that would blow past its band fans out instead (chapter 5)" (L18). Amortized batching is in-scope per §5/§8 (it is a unit/dispatch property), so its presence here is correct, not encroachment.
- **ch6 (audit/freeze depth):** Correctly previewed-only with explicit deferral (L52). Does not explain structural independence mechanics.
- **ch7 (resilience):** Untouched. No retry/re-hydrate mechanics.

---

## Summary

`HIGH: 0 · MEDIUM: 0 · LOW: 2 · Verdict: PASS`

LOW-1: "store" as a role-noun is lighter in ch3 than ch2 (cosmetic; chapter is unit/ledger-scoped).
LOW-2: ch3 says workers "return a structured result" (L42) where ch2 says "terse structured result"; drop of "terse" is cosmetic, same concept.
