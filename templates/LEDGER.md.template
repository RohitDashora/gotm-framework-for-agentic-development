---
project: <project-name>
last_updated: <YYYY-MM-DD>
---

# Project ledger

## Mission

<one-sentence mission: what done looks like>

## Active unit

<!-- A POINTER to the current unit only — one line, overwritten each turn.
     NOT a growing stack of dated status banners. The recovery log lives in
     `## Recent updates` below, and there is exactly one of it. See PROTOCOL.md
     -> Resilience -> "One recovery log, one ordering". -->

**U1** — <first unit title>

## Units

> Grouping by phase (e.g., "Foundation", "Drafting", "Wrapping") is convenience only. There is no hierarchy — only an ordered list of atomic units.

| ID | Title | Inputs | Output | Status | Audit |
|---|---|---|---|---|---|
| U1 | <first unit title> | — | `<output/path>` | pending | — |
| U2 | <second unit title> | U1 | `<output/path>` | pending | — |

## Recent updates

<!-- THE recovery log — the single place a cold session reconstructs "where we
     are and why". NEWEST-FIRST: prepend each entry, so current state is on top
     and never requires scrolling. Do not start a parallel dated stack anywhere
     else (esp. under `## Active unit`) — one log, one ordering. -->

- <YYYY-MM-DD>: Project initialized.

<!--
Conventions:
- IDs are flat (U1, U2, U3...). No prefixes for hierarchy.
- Each row produces ONE named output file (atomicity rule). A module + its test
  file count as ONE unit (the natural grain) — atomicity is one deliverable, not
  literally one file.
- `Inputs` lists prior units whose outputs this one consumes (foundation gate).
- Status: pending | in_progress | done | superseded.
- `Audit`: — (n/a) | pending | deferred→U<n> | PASS→audits/U<id>.md |
  PASS-FINDINGS→audits/U<id>.md | FAIL→audits/U<id>.md | superseded by U<yy>.
  A unit may be CONSUMED downstream only when its Audit is PASS, PASS-FINDINGS,
  or deferred→U<n> (follow-up present). One audit + one report PER UNIT, from its
  OWN report (no "covered-by" stamps); audit by a DIFFERENT agent than the author
  (dispatch a fresh auditor subagent), promptly — don't let `pending` pile up.
  See PROTOCOL.md -> Audit gates.
- Done units are never edited. To revise, append a follow-on unit.
- A new unit is born `pending`/`in_progress`, NEVER `done` — flip to `done` only
  after its output exists (crash-safe ordering; avoids the immutability hook
  locking you out of the file you're producing). See PROTOCOL.md -> Resilience.
- Stamp `last_updated` (front matter) on every write-back, so the ledger's age
  is honest.
- ONE recovery log, ONE ordering: `## Recent updates` is the single recovery log
  and is newest-first; `## Active unit` is a one-line pointer, never a dated
  banner stack. A second de-facto log forming is a reconciliation smell.
  See PROTOCOL.md -> Resilience.
- A unit-table row is also machine-parsed (the immutability hook reads it
  positionally). Keep every `| U<n> |` row well-formed — correct column count,
  a valid status, a parseable Output cell — so a botched edit can't silently
  change what the hook freezes. See PROTOCOL.md -> session-start reconciliation.
- Off-mission artifacts (work that does not serve the mission) do NOT go in the
  Units table. Produce the file, then add a "Recent updates" line marked
  `(not a mission unit)`. See PROTOCOL.md -> Off-mission artifacts.
-->
