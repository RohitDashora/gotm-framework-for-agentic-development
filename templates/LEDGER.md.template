---
project: <project-name>
last_updated: <YYYY-MM-DD>
---

# Project ledger

<!-- The ledger is the DAG + the scheduler's state: the units, their dependency
     edges, and each unit's status. It is BORN TIERED — a hot `## Frontier`
     re-read every turn, and a cold `## Archive` pulled only on demand — so the
     file the driver re-reads each turn stays small and roughly constant in cost,
     however many units the project closes. Only the DRIVER writes this file;
     workers execute and return a terse structured result, the driver records it.
     THREE SURFACES. The CANONICAL ledger is a machine-native flat file, one line
     per unit — each line carrying that unit's `depends_on`, state, spec, read-set,
     and output key. THIS markdown table is the DERIVED, read-only human view
     rendered from it: never hand-edited, no two-way sync. The mission/recovery
     narrative (`## Recent updates`) is a SEPARATE, append-only prose surface.
     See PROTOCOL.md -> Audit gates / Resilience, and docs/03-work-as-a-dag.md. -->

## Mission

<one-sentence mission: what done looks like>

## Active unit

<!-- A POINTER to the current unit only — one line, overwritten each turn.
     NOT a growing stack of dated status banners. The recovery log lives in
     `## Recent updates` below, and there is exactly one of it. See PROTOCOL.md
     -> Resilience (one recovery log, newest-first). -->

**U1** — <first unit title>

## Frontier

<!-- THE HOT TIER — re-read every turn; keep it small. It holds only:
       - every `pending` / `in_progress` unit (the ready-set lives here),
       - the last ~N (≈10) closed units (the recent-completion window), and
       - any older closed unit that a still-open unit lists in `depends_on`
         (so the consumer can see its input's audit verdict without a cold read).
     A frontier row is an INDEX ENTRY, not a record of the work — the work
     itself lives in the output file, and the audit detail lives in its
     `audits/` report. When a closed unit ages past the window AND no open unit
     still cites it as an input, COMPACT it down to a one-line `## Archive`
     entry (see Conventions -> compaction). `depends_on` lists the units that gate
     this one — the dependency edges of the DAG and the sole carrier of ordering
     (foundation gate); a unit's read-set (what its worker actually reads) is a
     subset of `depends_on` and defaults to it. `Status` distinguishes the states
     (see Conventions). `Kind` types
     the unit so the audit picks the right verified-done dimensions (see
     Conventions). `Tier` is the static per-unit model tier the driver sets by
     the rubric — `Kind` largely predicts it (see Conventions). `Audit` holds the
     verdict + a pointer to the report. -->

| ID | Title | depends_on | Output | Kind | Tier | Status | Audit |
|---|---|---|---|---|---|---|---|
| U3 | <a task split into parallel subtasks> | U1, U2 | — | authoring | standard | verified-done | — |
| U3.1 | <subtask 1 — one deliverable> | U1 | `<output/path>` | authoring | economy | verified-done | PASS→audits/U3.1.md |
| U3.2 | <subtask 2 — one deliverable> | U1 | `<output/path>` | authoring | economy | verified-done | PASS→audits/U3.2.md |
| U3.3 | <subtask 3 — one deliverable> | U3.1, U3.2 | `<output/path>` | authoring | standard | verified-done | PASS→audits/U3.3.md |
| U11 | <blocker: "await external sign-off" — a conditional unit (node+edge), not a status flag> | — | — | authoring | economy | pending | — |
| U7 | <some pending unit (atom, not split)> | U3, U11 | `<output/path>` | authoring | standard | pending | — |
| U8 | <a module + its test — one deliverable> | U3 | `<src/thing.ts>`, `<src/thing.test.ts>` | authoring | standard | pending | — |
| U9 | <a dashboard/component — live-verify> | U6 | `<app/panel.tsx>` | ui | economy | pending | — |
| U10-learn | <harvest learnings from U3 subtree> | U3.1, U3.2, U3.3 | `<learnings/path.md>` | meta | standard | pending | — |
| U6 | <some closed unit in the recent window> | U2 | `<output/path>` | authoring | frontier | verified-done | PASS→audits/U6.md |

## Archive

<!-- THE COLD TIER — NOT on the hot path; pulled only on demand. Closed +
     verified units that have aged past the frontier window compact to ONE LINE
     each, newest-first. Keep the audit pointer so the gate stays checkable
     (a downstream consumer, or a reconciliation pass, can still confirm the
     input passed) — but drop everything else: the detail already lives in the
     output file, `audits/`, and `DECISIONS.md`. One line per unit:

       - U<n> — <title> · `<output/path>` · <status> · <verdict>→audits/U<n>.md

     When this section grows large it MAY move to a separate `LEDGER-ARCHIVE`
     file that is NOT read at session start (the frontier alone is the hot
     read); leave a one-line pointer here to that file if you split it out. -->

- U5 — <title of an aged-out closed unit> · `<output/path>` · verified-done · PASS-FINDINGS→audits/U5.md
- U4 — <title of an aged-out closed unit> · `<output/path>` · verified-done · PASS→audits/U4.md

## Recent updates

<!-- THE recovery log — the single place a cold session reconstructs "where we
     are and why". NEWEST-FIRST: prepend each entry, so current state is on top
     and never requires scrolling. This is a ROLLING WINDOW: keep ~the last 20
     entries; older ones roll into `## Archive` (or `LEDGER-ARCHIVE`) so the log
     doesn't grow monotonically. Do not start a parallel dated stack anywhere
     else (esp. under `## Active unit`) — one log, one ordering. -->

- <YYYY-MM-DD>: Project initialized.

<!--
Conventions:
- BORN TIERED. The ledger has a HOT `## Frontier` (open units + the recent-
  closed window + any closed unit a still-open unit still cites as an input) and
  a COLD `## Archive` (everything older, one line each). The driver re-reads the
  frontier every turn and the archive never — that is what keeps per-turn read
  cost flat as the project scales. Read the AUDIT FILE, not a fat ledger cell.
- SINGLE WRITER. Only the driver writes this file. Workers execute one unit and
  return a terse structured result (status + output pointer + index facts); the
  driver — the single writer — records it. This kills the v2 dup-row race by
  construction (no second context ever writes back). See docs/03-work-as-a-dag.md.
- PROVENANCE AND DEPENDENCY ARE TWO ORTHOGONAL, FIRST-CLASS RELATIONS.
  · PROVENANCE — IDs follow **decimal provenance** (U1, U2, U3, U3.1, U3.2, U3.3,
    U4...). When a Task splits, children are registered as decimals (U3.1, U3.2,
    ...). Decimals are **append-only** (siblings 2,3,4... never renumbered) and
    form an inert tree recording **where a unit came from — no execution meaning**.
  · DEPENDENCY — the DAG's **control flow** (what runs when) lives ONLY in the
    `depends_on` edges, the **sole carrier of ordering**; siblings with no inter-
    `depends_on` run in parallel. Reading decimal position as sequence destroys
    parallelism: **only `depends_on` matters**.
  A pure-container parent (U3) has no Output; it closes verified-done when all
  children (U3.1, U3.2, U3.3) pass.
- Each row produces ONE deliverable (atomicity rule). A module + its test file
  count as ONE unit (the natural grain) — atomicity is one deliverable, not
  literally one file.
- OUTPUT CELL = the machine's OWNERSHIP KEY (parsed by the immutability hook), so
  it is a validated micro-schema, not free prose:
    · Concrete backticked path(s) ONLY — `src/thing.ts`. NO globs, NO directories
      (`pipelines/`, `{server,client}/src`, `foo/*.ts`): a directory Output would
      over-freeze every file under it, incl. new/untracked ones, and a glob
      realpaths to a literal that matches no concrete file, so it never enters the
      ownership set. List the actual files.
    · Multi-file deliverable → COMMA-SEPARATED backticks: `a.ts`, `b.ts`. The hook
      parses EVERY backticked path, so the unit owns (freezes when closed / keeps
      editable when an active follow-on) ALL of them, not just the first.
    · NO raw `|` in ANY cell — a literal pipe shifts the columns and breaks the
      positional parse. Use `/` or escape it. (The hook fails safe on a mis-split
      row: it freezes nothing for that row rather than trust a shifted index.)
- `Kind` types the unit so the audit picks the right verified-done dimensions.
  Values (default `authoring`):
    authoring     — code/docs; the standard 7-point audit. Terminal: logic-verified.
    ui            — a rendered/visual artifact; audit rebuilds from CURRENT source
                    + objective machine checks (DOM/bbox/counts). Terminal: live-verified.
    eval          — a measurement/judge/comparison; audit checks HARNESS FAIRNESS
                    (equal-fidelity inputs, symmetric yardstick, A/B-swap for
                    position bias, confounds controlled/labelled). Terminal: live-verified.
    deploy-infra  — a deployed/stateful resource; audit exercises it end-to-end AS
                    the deployed identity (a hardcoded identity/endpoint is a finding).
                    Terminal: live-verified.
    data          — a produced dataset/table; audit re-queries it as the downstream
                    consumer. Terminal: live-verified.
    diagnosis     — a "why is X broken" unit; audit requires a CONTROLLED
                    reproduction (strip model/harness/config confounds) before any
                    root-cause is accepted. Terminal: live-verified.
    meta          — `learn` (harvest lessons) or `compact` (GC frontier).
                    Not work-deliverable units; driver-scheduled meta-operations.
                    Terminal: faithfulness-verified (learn) or no output (compact).
- `Tier` is a STATIC PER-UNIT LABEL — the worker's model tier — the driver sets
  by the rubric at plan time, like `Kind`. Values (default `standard`):
    economy   — small/fast model, low effort — mechanical leaf units.
    standard  — mid model, medium effort — routine authoring/keystones. (default)
    frontier  — strong model, high effort — eval/deploy-infra/diagnosis/live-verify/milestones.
  Default tiers: leaf → economy, keystone → standard, milestone → frontier.
  `Kind` override: Kind ∈ {eval, deploy-infra, diagnosis} → frontier (live-verify expensive).
  Escalation: ≥2 watchdog deaths (same Uxx) → +1 tier (economy→standard→frontier).
  The runtime binds `tier → (model, effort)` and sets it at spawn (the worker
  never reads its own tier). It is NOT a cost governor — just a static difficulty
  label; escalation handles mis-tiered units. See PROTOCOL.md → Tier binding.
- `depends_on` lists the units that gate this one — the dependency edges of the
  DAG and the **sole carrier of ordering**. A unit's read-set (what the worker
  actually reads) is a **subset of `depends_on`** and defaults to it: a unit may
  depend on an upstream for ordering without reading its output. A unit is
  dispatchable only once every `depends_on` is closed and past its audit gate
  (foundation = upstream topology, not a reminder).
- GRAPH INTEGRITY. The `depends_on` graph must stay ACYCLIC with no dangling or
  self edges; a `depends_on` pointing at a `superseded` unit is re-pointed to its
  successor. This validity is CHECKED (whole-graph validation — see PROTOCOL.md),
  never STORED — the graph carries only its edges, not a verdict about them.
- Status — the lifecycle, FIVE states:
    pending        — born, not yet dispatched (a leaf of the ready-set once
                     its inputs clear).
    in_progress    — a worker is executing it (or it is mid-recovery).
    authored-done  — a worker produced the output; the artifact EXISTS but has
                     NOT been independently checked. NOT consumable downstream
                     on its own — the producing worker never self-certifies.
    verified-done  — an INDEPENDENT audit/verification worker (auditor ≠ author)
                     checked the output and it passed. Only a separate, driver-
                     launched worker confers this; for deploy/infra/data units
                     the worker also exercises the live artifact as its real
                     consumer. This is the consumable terminal state.
    superseded     — scope changed; the unit was replaced by a follow-on.
- `Audit` holds the verdict + a pointer: `—` (n/a) | `pending` (authored-done,
  awaiting an auditor) | `PASS→audits/U<id>.md` | `PASS-FINDINGS→audits/U<id>.md`
  | `FAIL→audits/U<id>.md` | `superseded by U<yy>`. A unit reaches
  `verified-done` only on `PASS` / `PASS-FINDINGS`; `FAIL` keeps it blocking.
  A downstream unit may CONSUME an input only when that input is `verified-done`
  (Audit `PASS` / `PASS-FINDINGS`). One audit + one report PER UNIT, from its
  OWN report — by a DIFFERENT worker than the author. See PROTOCOL.md -> Audit
  gates.
- BORN pending/in_progress, NEVER done. A new unit (incl. a fix or follow-on)
  is registered `pending`/`in_progress`; flip to `authored-done` only after its
  output exists, and to `verified-done` only after an independent worker passes
  it (crash-safe ordering; avoids the immutability hook locking you out of the
  file you are producing). See PROTOCOL.md -> Resilience.
- **Pure-container units** (split parents) have **empty Output** (shown as `—`).
  A pure container (U3 above) is a parent that split into children (U3.1, U3.2, U3.3).
  It holds no Output of its own; it closes verified-done only when all children
  are verified-done. Any integration step is itself the final delegated child
  (the "reduce"), never done by the driver.
- **Verify-grain split:** Runtime kinds (deploy-infra, data, eval, diagnosis) MUST
  reach `live-verified` (exercised as real consumer) to pass; authoring/ui kinds
  reach `logic-verified` (checked against spec) and terminate there. A logic-only
  audit of a runtime unit = FAIL-as-UNVERIFIED, not a pass. Milestones force
  live-verified aggregation for runtime tasks.
- Closed units are never edited. To revise, append a follow-on unit.
- COMPACTION is an index op, not a freeze violation. At session-start reconcile,
  when the ledger crosses a size/count threshold, move aged-out closed+verified
  units from `## Frontier` to a one-line `## Archive` entry, and roll `Recent
  updates` past its window into the archive too. This is LOSSLESS — the audit
  pointer is kept, the detail already lives in the output/`audits/`/`DECISIONS.md`
  — so it does not touch any frozen output and is NOT an immutability violation.
- Stamp `last_updated` (front matter) on every write-back, so the ledger's age
  is honest.
- ONE recovery log, ONE ordering: `## Recent updates` is the single recovery log
  and is newest-first; `## Active unit` is a one-line pointer, never a dated
  banner stack. A second de-facto log forming is a reconciliation smell.
  See PROTOCOL.md -> Resilience.
- GRAPH-EVOLUTION TELEMETRY IS A SEPARATE, APPEND-ONLY SURFACE. Meaningful graph
  mutations (insert, split, add/remove/re-point an edge, supersede, status/audit
  transitions) are recorded there one entry per structural edit — each with its
  ACTOR and REASON — kept OUT of the unit rows so the ledger stays clean. It is
  INSTRUMENTATION, not a learning channel: captured here, never promoted to the
  cross-project pool. See PROTOCOL.md -> Graph-evolution telemetry.
- A frontier row is also machine-parsed (the immutability hook reads it by
  COLUMN NAME to decide what to freeze). Keep every `| U<n> |` row well-formed —
  correct column count (matching the header), a valid status, a parseable Output
  cell of concrete backticked path(s) — so a botched edit can't silently change
  what the hook freezes. The hook parses ALL backticked Output paths (multi-file
  ownership) and FAILS SAFE on a mis-split row (freezes nothing for it rather than
  trust a shifted index). See PROTOCOL.md -> Anti-drift & the freeze / Resilience.
- LOCKSTEP CAVEAT — a column change (a rename OR an insert) moves what the parsers
  read. Any runtime parser that reads ledger columns BY NAME or BY POSITION (the
  immutability hook, the compaction script) MUST be updated in the SAME change
  that lands this `Inputs → depends_on` rename, and RUN against a real ledger — a
  rename breaks a name-keyed parser even though positions hold, and inserting a
  column silently re-numbers every column after it, so a stale parser reads the
  wrong cells (freezes the wrong path, mis-reads Status). This is the v3.35
  ledger-shape lesson: never ship a column move without its parser update +
  a real-ledger parse test.
- ARCHIVE ENTRIES ARE ALSO MACHINE-PARSED, AND IN ONE CANONICAL SHAPE. The
  `## Archive` cold tier is a one-line BULLET per unit — never a table — in this
  exact form (fields separated by " · "; an adopter's immutability hook,
  compaction script, and v2->v3 converter all read it):
      - U<n> — <title> · `<output/path>` · <status> · <verdict>→audits/U<n>.md
  Every archive bullet MUST carry a parseable Status (the 3rd " · " field) and a
  backticked Output (the path) so the hook can still freeze an aged-out closed
  unit's output. A `verified-done` / `authored-done` / legacy `done` archive
  bullet IS frozen exactly like a frontier row; a `superseded` one is not. Keep
  the status token verbatim — don't downgrade `verified-done` to `done`.
- Off-mission artifacts (work that does not serve the mission) do NOT go in the
  Frontier table. Produce the file, then add a "Recent updates" line marked
  `(not a mission unit)`. See PROTOCOL.md -> Off-mission artifacts.
- PLATFORM BINDING: this template uses generic paths ("the store", `audits/`,
  `LEDGER-ARCHIVE`). The concrete layout — e.g. a `.gotm/` subfolder, the split-
  file convention, a compaction script — is an adopter/platform binding, not part
  of the framework. Adjust the relative paths to your layout.
-->
