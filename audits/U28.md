# Audit report — U28 — independent audit of U20–U27 + U29–U32 — 2026-06-11

> **Produced by an independent auditor agent, not the author.** This audit was run
> by a dispatched fresh context that did not author the feedback-integration
> (U20–U27) or audit-gate (U29–U32) edits. It received only the targets, the oracle
> (`LEDGER.md`, `DECISIONS.md` D12–D15, `prompts/audit.md`), and the checks below —
> never the authoring session's transcript or reasoning. Per the independence rule
> in `prompts/audit.md` §"Independence (non-negotiable)" and D15, this satisfies the
> non-self-marking requirement.

## Target audited

Units: U20, U21, U22, U23, U24, U25, U26, U27 (feedback-integration) and
U29, U30, U31, U32 (audit-gate).

Files:
- `docs/01-what-is-gotm.md`
- `docs/02-what-agents-are-missing.md`
- `docs/03-gotm-with-agents.md`
- `templates/PROTOCOL.md.template`
- `templates/LEDGER.md.template`
- `templates/DECISIONS.md.template`
- `README.md`
- `CONTRIBUTING.md`
- `prompts/audit.md`

Oracle: `LEDGER.md` (U20–U32 promised outputs), `DECISIONS.md` (D12–D15),
`prompts/audit.md` (severity tiers + report shape).

Audit kinds combined per the dispatch: existence + structure + render/consistency +
ledger-fidelity + the repo neutrality invariant (no shipped runtime).

## Findings

### HIGH severity

None.

Every promised output exists, every promised section/column/subsection is present,
section numbering renders contiguously, all tables that gained a column have a
matching separator row, no broken intra-doc references were found, the neutrality
invariant holds, and the LEDGER claims match disk. No finding blocks downstream
consumption.

### MEDIUM severity

None.

### LOW severity

- **README doc word-count claim is ~3% high (within the "~" tolerance).**
  `README.md:26` states the `docs/` directory is "~4,800 words"; the measured total
  across the three chapters is 4,942 words (`docs/01`=1,128, `docs/02`=1,595,
  `docs/03`=2,219). The "~" qualifier covers this gap and the U26 promise was a doc
  word count "~4,800", so this is cosmetic only — flag if a precise count is ever
  wanted. No action required.

### UNVERIFIED

None. All ten dispatched checks resolved to a clear pass/fail against on-disk state
and the oracle. No missing context, no ambiguous spec.

## Detailed check results (all PASS)

**1. Existence.** All nine target files exist at their stated paths. `audits/` contains
`U17.md` (prior) and now this report; no other audit files were expected.

**2. Structure (per-unit promises).**
- U20 — `docs/02` adds `## 8. The discipline rules rely on memory` (line 94) and
  `## 9. Session ends are not always graceful` (line 102); conclusion renumbered to
  `## 10. The agent cannot fix this alone` (line 110). PASS.
- U21 — `docs/03` adds `## 7. Keeping the discipline honest` (line 99); the reconcile
  step is present in §3 ("The agent reconciles the ledger against what is actually on
  disk", line 56); tail renumbered to `## 8. What still requires the agent` (line 109)
  and `## 9. What's next` (line 117). PASS.
- U22 — `docs/01` Ledger primitive carries the recovery-point sentence ("Because the
  ledger *is* the project's state, it is also its recovery point: the files on disk —
  not the chat history — must be enough to resume…", line 20). PASS.
- U23/U29 — `templates/PROTOCOL.md.template` contains `## Audit gates` (line 63),
  `## Anti-drift safeguards (operational)` (line 75), `## Resilience — no context loss
  across any session end` (line 99), `## Off-mission artifacts` (line 125), a Layout
  note (line 7), a session-start checklist with a reconciliation step (step 4, line 18),
  and an "Audit-gate lint" bullet (line 120). PASS.
- U24/U30 — `templates/LEDGER.md.template` unit table has an `Audit` column (header
  line 20); conventions describe Audit values and the consume-gate (lines 35–38:
  "A unit may be CONSUMED downstream only when its Audit is PASS or deferred→U<n>").
  PASS.
- U25 — `templates/DECISIONS.md.template` describes the Status-line edit as the
  documented mechanism ("that one Status-line edit is the documented mechanism, not a
  substantive rewrite", line 3). PASS.
- U26 — `README.md` primitives summary mentions anti-drift safeguards and resilience
  (line 13); `.gotm/` layout appears in the quickstart (line 40) and the repo tree
  (line 28); doc word count claim present (line 26). PASS.
- U27 — `CONTRIBUTING.md` points enforcement at adopter tooling rather than forbidding
  the safeguards ("The protocol's anti-drift safeguards are paste-able prompts and are
  in scope; the *mechanical enforcement* of them is a platform binding that lives in
  adopter tooling", line 33). PASS.
- U31 — `prompts/audit.md` contains `### Independence (non-negotiable)` (line 14) and an
  "After the audit" step that stamps the ledger Audit cell PASS/FAIL ("Stamp the ledger
  `Audit` cell … `PASS→audits/<Uxx>.md` … else `FAIL→audits/<Uxx>.md`", line 107). PASS.
- U32 — `docs/03` §6 strengthened with independence-by-dispatch + the gate (line 95:
  "independence … run by a context that did *not* author the unit … dispatched … the
  **gate** … a downstream unit consumes an input only once that input has passed").
  PASS.

**3. Render / consistency.**
- `docs/02` H2 numbers are contiguous 1..10 with no duplicates or gaps. PASS.
- `docs/03` H2 numbers are contiguous 1..9 with no duplicates or gaps. PASS.
- `templates/LEDGER.md.template` Audit table: header row has 6 cells, separator row has
  6 `---` cells — matched. PASS. Other tables in `PROTOCOL.md.template` (ratification
  ladder 3×3, common-moves 2×2) have matched separators. PASS.
- Intra-doc references resolve correctly: `docs/03` §6 cites "the §5 failure" → `docs/02`
  §5 is "Self-marking, not self-auditing" (correct); `docs/03` §7 cites "§8 there" and
  "§9 there" → `docs/02` §8 ("rules rely on memory") and §9 ("non-graceful ends")
  (both correct). The `docs/03` intro paragraph (line 8) enumerates the two new gaps in
  prose, consistent with the renumbered `docs/02`. No broken references. PASS.
- **Neutrality invariant for this repo (no shipped runtime).** Confirmed absent:
  `gotm-immutability.py`, `.claude/settings.json`, `CLAUDE.md.template` (root and
  `templates/`). `prompts/audit.md` describes enforcement via dispatch only and never
  ships a runtime; `PROTOCOL.md.template` line 97 states "runtime enforcement bindings
  belong in adopter tooling … not in this framework"; `CONTRIBUTING.md` line 33 keeps
  mechanical enforcement out of scope. No stale references to deleted `meta/`,
  `examples/`, or `01-why` remain. PASS.

**4. Ledger fidelity.** LEDGER rows U20–U32 list the same outputs verified above and all
carry Status `done` (U28 itself `pending`, correctly). The `Audit` column for U20–U32 is
`pending` — accurate, since this is the first independent audit and no cell has yet been
stamped (the stamp is the orchestrator's post-audit action per `prompts/audit.md` line
107, not the auditor's). D15's stated consequences match the edits on disk: the `Audit`
column landed in `LEDGER.md.template`, the *Audit gates* section + audit-gate lint landed
in `PROTOCOL.md.template`, the independence preamble + stamp step landed in
`prompts/audit.md`, and `docs/03` §6 was strengthened — all present. D13/D14 consequences
(docs/02 §8–§9, docs/03 §7, the Layout note, README mentions) also match. PASS.

## Summary
HIGH: 0 · MEDIUM: 0 · LOW: 1 (within tolerance) · UNVERIFIED: 0
Recommendation: **passes; optional polish.** All U20–U27 + U29–U32 outputs exist,
are structurally complete, render cleanly, and match the ledger and D12–D15. The single
LOW (README word-count ~3% high, covered by the "~") is cosmetic. No HIGH findings →
the orchestrator may stamp the audited units' `Audit` cells `PASS→audits/U28.md`.
Downstream consumption (including the Q2 visibility flip and any commit/push) is
unblocked.
